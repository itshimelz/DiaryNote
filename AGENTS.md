# AGENTS.md — Agent Working Rules & UI Component Registry

**Document Version:** 1.0.0  
**Target Application:** DiaryNote Desktop (Tauri + Rust + React + TypeScript)  
**Applicability:** Mandatory for all AI agents, subagents, and automated assistants operating on this codebase.

---

## 1. Mandatory Agent Rule: UI Component Modification Registry

> [!IMPORTANT]
> **RULE #1 (UI CHANGE MANIFEST):**
> Whenever an agent modifies, refactors, creates, or deletes **any UI component, modal, hook affecting rendering, or stylesheet** (`src/components/**`, `src/hooks/**`, `src/index.css`), the agent **MUST immediately update the UI Component Modification Registry below in this document (`AGENTS.md`)**.
>
> This enables instant identification of visual regressions, broken event handlers, or UI mismatches across phases.

### Required Registry Entry Schema
For every modified UI file, the agent must document:
1. **Phase & Task ID** (e.g., `Phase 1 - Task 1`)
2. **File Path** (e.g., `src/components/Modals/AppModals.tsx`)
3. **UI Elements Affected** (e.g., Modal overlay, Paste dialog, Confirm button)
4. **Nature of Change** (Props, State flow, DOM structure, Tailwind/CSS classes, Event handlers)
5. **Expected Visual & Functional Behavior** (What the user should see and how it responds)
6. **Regression Verification Checklist** (Exact UI interactions to test)

---

## 2. Mandatory Operational Commands & Tool Rules

> [!CAUTION]
> **RULE #2 (NO DEV SERVER EXECUTION):**
> **DO NOT EVER run `npm run dev`, `bun dev`, `vite`, or launch background development servers.**
> DiaryNote is tested and validated via static checks, unit tests, and production build verification (`npm run lint`, `npm test`, `npm run build`, and `cargo check`). Do not spawn long-running server processes.

> [!IMPORTANT]
> **RULE #3 (ALWAYS CHECK LINT WITH OXLINT):**
> **Always execute `npm run lint` before concluding any task.**
> `npm run lint` is configured with `oxlint` as the default ultra-fast linter combined with TypeScript strict typechecking (`oxlint && tsc --noEmit`).
> Tasks must never be marked complete if `npm run lint` reports any errors.

---

## 3. Active UI Component Modification Registry

*Agents must append and update entries here during task execution.*

| Phase / Task | Component / File Path | UI Elements Affected | Specific Changes Made | Expected Visual / Behavioral Result | Verification & Regression Check |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Phase 1 (Task 1)** | `src/components/Modals/AppModals.tsx` | Paste confirmation dialog, Note creation triggers | Replaced direct `setNotes` calls with repository methods; wired dirty persistence settlement. | Paste modal closes cleanly; pasted card renders immediately with persistent save indicator. | Verify paste confirmation creates card and survives immediate reload (`Ctrl+R`). |
| **Phase 1 (Task 1)** | `src/components/StatusBar.tsx` | Save status indicator badge | Bound save status to actual IndexedDB promise settlement instead of optimistic timer. | Badge displays "Saving..." during I/O and "Saved" only after storage acknowledgement. | Verify save badge shows error banner if storage fails. |
| **Phase 1 (Task 2)** | `src/App.tsx` | Root canvas action dispatchers | Routed history undo/redo through persistence coordinator. | Visual undo/redo on canvas remains synchronized with local database. | Press `Ctrl+Z` to restore deleted note; verify note remains present after reload. |
| **Phase 2 (Task 5)** | `src/components/BatchActionBar.tsx` | Batch export, batch delete, align/theme popovers | Integrated `authorizeNotes()` checks before exporting or deleting selected cards. | Locked notes prompt for passcode or are safely excluded with clear UI badge notice. | Select 2 unlocked notes + 1 locked note; click Export; verify locked note is not leaked. |
| **Phase 2 (Task 5)** | `src/components/NoteCard/NoteHeader.tsx` | Copy button, lock status icon | Guarded clipboard copy button with authorization check. | Clicking copy on a locked card prompts "Unlock note to copy" instead of copying plaintext. | Click copy icon on locked card; verify clipboard remains protected. |
| **Phase 2 (Task 5/6)** | `src/lib/sqliteStorage.ts` & `src/lib/storage.ts` | IndexedDB persistence & JSON backup export | Integrated at-rest envelope encryption for locked notes; guarded full backups with user notification. | Locked note bodies stored as AES-256-GCM ciphertext on disk; full backups redact locked entries if unauthenticated. | Inspect IndexedDB and export backup without unlocking; verify plaintext is never exposed. |
| **Phase 2 (Task 5/9)** | `src/hooks/useNotesManager.ts` | Deletion handlers & ID generation | Awaited deletion promises with `isSaving`/`saveError` settlement; standardized IDs to `crypto.randomUUID()`. | Direct $O(1)$ deletion updates status bar; new notes and journal entries have collision-resistant UUIDs. | Create note and delete note; verify status bar indicates save settlement and ID matches UUID regex. |
| **Phase 2 (Task 6)** | `src/components/Modals/SecurityModal.tsx` | Master passcode setup, unlock dialog | Redesigned with unified tokens (rounded-sm, monochromatic primary/secondary buttons, portal mounting, Escape listener). | Passcode setup and unlock dialogs match the Dock and Canvas Preferences style cleanly. | Test passcode unlock and recovery; verify responsive layout and escape dismissal. |
| **Phase 2 (Task 8)** | `src/components/Modals/AISettingsModal.tsx` | AI Feature Settings dialog | Redesigned with unified design tokens (rounded-sm, tactile pill toggle, 28-day activity heatmap, portal mounting, Escape listener). | Visual layout, colors, and button hover styles match the Dock and Canvas Preferences perfectly. | Open AI Settings; toggle services; test API key; verify clean escape dismissal and test feedback. |
| **Phase 3 (Task 11)** | `src/components/Modals/ImportPreviewModal.tsx` *(New)* | Staged backup import preview modal | Staged import modal with portal mounting (`z-[60]`), Escape dismissal, and duplicate ID resolution options. | User sees visual breakdown of backup contents unobstructed before committing to database. | Import backup with duplicate IDs; select "Keep Both"; verify both cards appear on canvas. |
| **Phase 3 (Task 13)** | `src/components/Modals/JournalCalendarModal.tsx` | Calendar date grid, streak counter | Redesigned with unified tokens, query `isDailyEntry && entryDate`, and portal mounting. | Calendar highlights only genuine daily entries with sleek monochromatic indicators. | Create note titled "2026-08-14 Planning"; verify calendar does not mark it as a daily entry. |
| **Phase 3 (Task 14)** | `src/components/Modals/CanvasSettingsModal.tsx` *(New)* | Preferences window dialog | Two-column desktop settings window with tabs (Canvas, Appearance, Data, AI, About) and auto-dismiss on import. | Users configure workspace preferences smoothly; clicking import dismisses settings cleanly for staged preview. | Open settings, switch tabs, click Import Backup, verify file picker and staged preview appear unobstructed. |
| **Phase 3 (Task 14)** | `src/components/Modals/DeleteConfirmationModal.tsx` | Deletion confirm dialog | Redesigned with portal mounting, escape dismissal, and matching button tokens. | Sleek danger confirmation popup with note preview count. | Select note, press delete, verify modal renders and closes cleanly. |
| **Phase 3 (Task 14)** | `src/components/Modals/KeyboardShortcutsModal.tsx` | Shortcuts cheatsheet dialog | Redesigned with unified kbd badges, search filter bar, and portal mounting. | Clean shortcuts table matching desktop design language. | Open shortcuts (Ctrl+/); filter search; verify smooth navigation. |
| **Phase 3 (Task 14)** | `src/components/Modals/PasteConfirmModal.tsx` | Clipboard paste modal | Redesigned with portal mounting and monochromatic action buttons. | Paste modal matches Dock and Canvas Preferences style. | Paste clipboard text; verify modal styling and creation. |
| **Phase 3 (Task 14)** | `src/components/Modals/SearchModal.tsx` | Global search dialog | Redesigned with portal mounting, clean tags bar, and unified result highlighting. | Global note search renders with zero visual artifacts. | Press Ctrl+K; type search query; use arrow keys to navigate. |
| **Phase 3 (Task 14)** | `src/components/Modals/AboutModal.tsx` | About & Diagnostics dialog | Redesigned with unified feature cards and monochromatic buttons. | Clean release channel and diagnostic info popup. | Open About; click check update; verify clean indicator. |
| **Phase 3 (Task 14)** | `src/components/CanvasControls.tsx` | Settings trigger, control dock | Extracted settings window into `CanvasSettingsModal`; bound file selection to auto-dismiss settings modal. | Clean, compact docked control bar without DOM leakage or stacked modal collisions. | Open settings from dock; verify toggle states persist and import triggers correctly. |
| **Phase 4 (Task 15)** | `src/components/Common/AccessibleDialog.tsx` *(New)* | Modal overlay container primitive | Replaces unsemantic `div` overlays with semantic `<dialog>`, focus trap, and Escape listener. | Accessible dialogs capture keyboard focus; pressing `Escape` closes modal and restores focus. | Navigate entire modal using `Tab` and `Shift+Tab`; verify focus cannot escape to background. |
| **Phase 4 (Task 15)** | `src/components/NotesSidebar.tsx` | Note list drawer rows | Changed note items from `<div>` to semantic `<button>` tags; added list virtualization. | Sidebar keyboard navigable with arrow keys / Tab; smooth scrolling across thousands of notes. | Test navigating sidebar using only keyboard; verify DOM node count remains <30. |
| **Phase 4 (Task 16)** | `src/components/InfiniteCanvas.tsx` | Canvas viewport, rubber-band selector, minimap | Moved 2D HTML5 Minimap to top-right corner (`top-4 right-4`); normalized shadows to `shadow-sm`. | Minimap floats in top-right without colliding with bottom command dock or status bar. | Open canvas; verify minimap is in top-right and bottom dock is unobstructed. |
| **Phase 4 (Task 16)** | `src/hooks/useNoteResize.ts` | Card resize handles | Converted resizing to direct DOM style transforms (`style.width/height`) during drag. | Card resizes smoothly at 60 FPS without triggering canvas-wide re-renders. | Drag resize handle; monitor CPU in DevTools (should remain <5%). |
| **Phase 4 (Task 17)** | `src/components/NoteCard/index.tsx` | NoteCard root component | Decoupled `allNotes` prop; isolated `React.memo` comparator to note-specific fields. | Modifying or typing in Note A re-renders *only* Note A. | Type rapidly in one note; verify other visible cards do not re-render. |
| **Phase 4 (Task 18)** | `src/components/ErrorBoundary.tsx` *(New)* | Root error boundary fallback | Top-level crash recovery screen with "Emergency Backup Export" and "Reload" buttons. | Prevents blank white screens on runtime errors; lets users recover all notes safely. | Throw test error; verify emergency export downloads intact JSON backup. |
| **Hotfix (Double-Click Creation)** | `src/components/InfiniteCanvas.tsx` & `src/hooks/useCanvasTransform.ts` | Canvas double-click note creation positioning | Passed raw client coordinates from `handleDoubleClick` and bound default note dimensions to `DEFAULT_NOTE_WIDTH`/`DEFAULT_NOTE_HEIGHT`. | Double-clicking or right-clicking anywhere on the canvas creates and centers the new note precisely at the mouse pointer, even when panned or zoomed. | Double click canvas at various pan offsets and zoom levels (50%, 100%, 200%); verify note card is centered under cursor without shooting off-screen. |
| **Hotfix (Layout & Groups & Persistence)** | `src/utils/layoutUtils.ts`, `src/components/InfiniteCanvas.tsx`, `src/hooks/useNotesManager.ts`, `src/components/Modals/AppModals.tsx` | Align Top calculation, Group Frame event wiring, bringToFront zIndex persistence, multi-duplicate selection | Fixed `alignTop` to use `min(y)`; wired `onSelectMultipleNotes`, `onDragStateChange`, `snapToGrid` to `GroupFrame`; marked `zIndex` dirty in `bringToFront`; batched multi-note duplication. | Note alignment aligns smoothly; clicking/dragging group badges selects and moves all group notes; note layering survives restart; batch duplicating selects all new notes. | Select 3 notes -> click Align Top; click group badge to drag; change note layer and reload; duplicate multiple notes. |
| **Hotfix (Right-Click Multi-Select Menu)** | `src/hooks/useNoteDrag.ts`, `src/hooks/useNoteResize.ts`, `src/components/NoteContextMenu.tsx` | Right-click mousedown selection preservation & multi-note context menu | Added `e.button !== 0` check to `useNoteDrag` and `useNoteResize` to prevent right-clicks from firing drag mouseup deselect; enabled Duplicate Note(s) on multi-selection context menu. | Right-clicking on any selected note preserves the multi-note selection and displays the multi-note context menu ("X notes selected", group, theme, duplicate, delete). | Select 2+ notes -> right click any selected note -> verify all notes remain selected and multi-note context menu opens. |
| **Hotfix (Single & Batch Unlock)** | `src/App.tsx`, `src/components/Modals/AppModals.tsx`, `src/hooks/useAppUIState.ts` | Context menu single and batch note unlocking | Wired `handleLockSelectedNotes` to detect locked status; triggers master session check or prompts `SecurityModal` in `'unlock'` mode and executes batch/single `isLocked: false` resolution upon authentication. | Clicking "Unlock Note(s)" in the context menu or pressing `Ctrl+L` prompts passcode (if unauthenticated) or instantly unlocks notes. | Select locked note(s) -> right click -> click "Unlock Note(s)" -> verify modal unlocks and updates card lock status. |
| **Hotfix (Slash & Mention Menu UI)** | `src/components/NoteCard/SlashCommandMenu.tsx`, `src/components/MentionAutocomplete.tsx`, `src/components/NoteContextMenu.tsx` | Slash command menu, @mention autocomplete, and context menu | Standardized to strict `rounded-sm` and `shadow-sm` tokens; fixed active keyboard highlight using monochromatic modal color tokens (`bg-slate-800 text-white font-medium` / `bg-slate-100 text-slate-900 font-medium`); improved parent clamping and directional flipping. | Pressing `ArrowUp`/`ArrowDown` visibly highlights each option with monochromatic active background; menu positions cleanly inside note editor bounds with compact, subtle `rounded-sm` corners. | Type `/` or `@` in a note editor -> press `ArrowDown`/`ArrowUp` -> verify active row highlight moves smoothly and menu fits inside card with subtle borders and shadows. |
| **Hotfix (Flawless Note Resizing)** | `src/hooks/useNoteResize.ts`, `src/components/NoteCard/index.tsx`, `src/components/NoteCard/NoteChecklist.tsx` | Note card dimensions, zero scrollbars & natural content height | Removed all inner scrollbars (`overflow-y-auto`); card uses `minHeight` so text never scrolls inside cards; resizing width naturally wraps text while resizing minHeight accommodates user-defined spatial layouts. | Note cards render all text completely without inner scrollbars; dragging bottom-right handle smoothly adjusts width and minHeight at 60 FPS. | Type multi-paragraph text or resize card; verify zero internal scrollbars and smooth 60 FPS resizing. |
| **UI Standardization (Phase 1)** | `src/components/ui/*` (`Button`, `IconButton`, `Icon`, `Input`, `Textarea`, `Switch`, `Dialog`, `Tabs`, `Menu`, `Kbd`, `Badge`, `tokens.ts`) | UI Design System Primitive Foundation with Hugeicons | Created lightweight, typed, accessible UI primitives encapsulating Hugeicons (1.5px stroke weight), `rounded-sm` (4px) tokens, focus rings, and dark/light monochromatic surfaces. | Standardized primitives render with desktop-native crispness, full keyboard/ARIA support, and 0 external UI framework overhead. | Run vitest suite in `src/components/ui/__tests__`; all 79 tests pass with 0 lint errors. |
| **UI Standardization (Phase 2)** | `src/components/Modals/DeleteConfirmationModal.tsx`, `PasteConfirmModal.tsx`, `AboutModal.tsx`, `KeyboardShortcutsModal.tsx` | Confirmation and informational dialogs | Refactored to consume `Dialog`, `Button`, `Input`, `Textarea`, `Kbd`, `Badge`, and Hugeicons (`Alert02Icon`, `Delete02Icon`, `ClipboardIcon`, `KeyboardIcon`, `InformationCircleIcon`, `RotateLeft01Icon`). | Consistent `rounded-sm` geometry, monochromatic surfaces, full keyboard focus trap, and clean Escape dismissal. | Open delete, paste, about, and shortcuts dialogs; verify ESC dismissal, button hover highlights, and shortcut filter. |
| **UI Standardization (Phase 3)** | `src/components/Modals/SecurityModal.tsx`, `ImportPreviewModal.tsx`, `JournalCalendarModal.tsx`, `CanvasSettingsModal.tsx`, `AISettingsModal.tsx`, `SearchModal.tsx` | Settings, Data & Security Modals | Refactored with `Dialog`, `Input`, `Button`, `IconButton`, `Switch`, `Badge`, `Kbd`, and Hugeicons (`SecurityLockIcon`, `Key01Icon`, `Calendar03Icon`, `FireIcon`, `SlidersHorizontalIcon`, `SparklesIcon`, `Search01Icon`, `Download01Icon`, `Upload04Icon`). | Zero CSS drift; unified 2-column preferences window, accessible daily entry calendar grid, AES-256 encrypted AI settings with 28-day activity heatmap, and high-performance search. | Open security setup/unlock, preferences tabs, backup staged preview, daily calendar, and search modal; verify all keyboard navigations and tests pass. |
| **UI Standardization (Phase 4)** | `src/components/CanvasControls.tsx`, `StatusBar.tsx`, `BatchActionBar.tsx`, `NoteContextMenu.tsx`, `NotesSidebar.tsx`, `MentionAutocomplete.tsx`, `NoteCard/*`, `GroupFrame.tsx`, `ErrorBoundary.tsx`, `UpdateAlertBanner.tsx`, `AccessibleDialog.tsx` | Bars, Menus, Floating Docks, Note Cards & Complete Lucide Removal | Migrated 100% of application iconography to Hugeicons (1.5px stroke weight); standardized all interactive elements to `rounded-sm` (4px), monochromatic hover surfaces, accessible dialogs; removed `lucide-react` dependency completely; configured `@hugeicons` vendor chunk splitting in `vite.config.ts`. | Pristine, unified desktop aesthetic across every toolbar, dock, context menu, autocomplete dropdown, sidebar drawer, and card; zero styling divergence or residual legacy icon packages. | Verified with `npm test` (18 suites, 79/79 passed), `npm run lint` (0 errors), `npm run build` (clean bundle with vendor-icons chunk), and `cargo check`. |
| **UI Standardization (Phase 5)** | `src/components/NoteCard/NoteHeader.tsx`, `src/index.css`, `src/hooks/useCanvasTransform.ts`, `src/components/Modals/*`, `src/components/ui/*` | NoteCard Header Action Icons & Full Light/Dark Modal Theme Adaptability | Configured Tailwind v4 class-based `@custom-variant dark`; synchronized `document.documentElement` `.dark` / `.light` states; updated all `NoteHeader` action icons (Mood, Pin, Group, Share, Delete, More) to `size="lg"`; ensured 100% modal & UI component adaptiveness across Light and Dark theme modes. | Note card header icons render at crisp `lg` (20px) dimension; modals and dialogs adapt instantaneously between light and dark modes with monochromatic palettes. | Switch theme between Light and Dark in Canvas Settings; open all modals; verify clean contrast, icon sizes, and zero visual artifacts. |
| **UI Standardization (Phase 6)** | `src/components/ui/Dialog.tsx`, `src/components/Modals/*` | Modal & Dialog Widths and Inside Padding | Expanded Dialog container padding to `p-6 sm:p-7`; widened all modal widths (`AISettingsModal` to `max-w-2xl`, `CanvasSettingsModal` to `max-w-3xl`, `SecurityModal`/`JournalCalendarModal`/`AboutModal` to `max-w-lg`, `KeyboardShortcutsModal` to `max-w-2xl`, `ImportPreviewModal`/`PasteConfirmModal` to `max-w-xl`); added `px-1 pr-2` scrollbar buffer. | UI components breathe comfortably with generous internal padding away from modal borders and scrollbars; wider dialogs display multi-column forms and cards with zero crowding. | Open AISettings, CanvasSettings, and other modals; verify spacious layout, proper scrollbar margins, and zero edge clipping. |
| **Hotfix (Note Popover & Native Style Picker)** | `src/components/NoteCard/NoteToolbar.tsx`, `NoteStylePicker.tsx`, `NoteHeader.tsx`, `index.tsx` | Bottom Toolbar 3-dot Menu, Native Style/Typography Picker & Header Popovers | Set `overflow-visible` on `.note-card`; anchored `NoteStylePicker`, More Menu, and Header Popovers directly in note canvas space; added native OS `<select>` font picker and compact theme grid. | Menus and style picker transform naturally with notes during canvas pan/zoom without screen-fixed disconnects or clipping; font picker opens OS native list without stretching picker container. | Open style picker on zoomed/panned notes; test font selection and theme tiles across light and dark cards; verify zero layout stretching or screen disconnect. |
| **UI Standardization (Goal: Single Source of Truth)** | `src/components/ui/*`, `BatchActionBar.tsx`, `CanvasControls.tsx`, `StatusBar.tsx`, `NotesSidebar.tsx`, `GroupFrame.tsx`, `ErrorBoundary.tsx`, `UpdateAlertBanner.tsx`, `SearchModal.tsx`, `NoteChecklist.tsx`, `NoteToolbar.tsx`, `NoteHeader.tsx`, `NoteContextMenu.tsx` | Universal application UI primitives and controls | Standardized 100% of application components to consume single source of truth `@ui` primitives (`Button`, `IconButton`, `Icon`, `Input`, `Select`, `SegmentedControl`, `Switch`, `Checkbox`, `Dialog`, `Tabs`, `Menu`, `MenuItem`, `MenuDivider`, `MenuGroupHeader`, `Tooltip`, `Badge`, `Kbd`, `tokens.ts`); eliminated ad-hoc raw buttons and menus. | Every toolbar, bottom dock, sidebar drawer, status chip, context menu, search modal, error fallback, update banner, and note control renders with unified tokens, full keyboard accessibility, and 0 styling divergence. | Run `npm test` (18/18 suites passed, 83/83 tests), `npm run lint` (0 warnings, 0 errors), `npm run build` (clean Vite bundle), and `cargo check`. |
| **Dock Integration (Batch Action Bar)** | `src/components/BatchActionBar.tsx`, `src/App.tsx` | Batch action bar & bottom dock integration | Removed independent fixed positioning from `BatchActionBar`; integrated it directly into the top tier of `bottom-dock-container` in `App.tsx` with smooth height/opacity animation (`[0.16, 1, 0.3, 1]`) and border-b separator. | When 2+ notes are selected, the dock smoothly expands upward with the batch action bar integrated seamlessly as the dock header; deselecting smoothly collapses the dock. | Select 2 notes; verify batch action bar expands seamlessly inside the dock with matching glassmorphism and borders; click deselect and verify smooth collapse. |
| **Hotfix (Dynamic Marquee Selection)** | `src/components/InfiniteCanvas.tsx`, `src/components/NoteCard/index.tsx` | Rubber-band canvas marquee multi-note selection | Pre-measured dynamic rendered note card dimensions (`offsetWidth`/`offsetHeight`) once on drag start; used accurate 2D AABB intersection checking. | Notes are selected immediately as soon as the rubber-band selection box touches ANY part of the card (top, bottom, or sides), regardless of dynamic text expansion or zoom levels. | Drag selection rectangle across top, middle, or bottom edge of any note; verify note selects instantaneously with zero lag. |
| **Storage Terminology & Import Normalization (Point 4)** | `src/components/StatusBar.tsx`, `src/App.tsx`, `src/hooks/*`, `src/lib/*`, `README.md` | Status bar storage badge, import layers, docs & tests | Replaced misleading "SQLite" badge with "IndexedDB Engine"; refactored all imports across hooks, components, and tests to `indexedDbStorage`; marked `sqliteStorage.ts` deprecated for upcoming native Rust SQLite engine; corrected README metadata. | Status bar accurately reflects "IndexedDB Engine"; all storage imports resolve strictly to `indexedDbStorage`; clean separation for future native Rust SQLite backend. | Run `npm test` (all 20 test suites, 89/89 tests pass), `npm run lint` (0 errors), `cargo check`. |
| **Typography Scaling (Note Card Header)** | `src/components/NoteCard/NoteHeader.tsx` | Note card title (input and h3) and date/metadata subheader | Increased note title font size to `text-base sm:text-lg tracking-tight font-bold` (18px) and date subheader from `text-[10px]` to `text-xs` (12px) with `mt-0.5`. | Note titles are prominently readable and distinguished from body copy at 100% zoom; date & group subheader is clearly legible. | Open canvas at 100% zoom; verify note titles and timestamps are crisp and comfortably readable without feeling diminutive. |
| **Status Bar Streak Harmonization** | `src/components/StatusBar.tsx` | Journal streak indicator button (FireIcon & streak count) | Harmonized fire icon and streak text color with status bar monochromatic theme tokens (`defaultIconClass` & standard text). | Streak badge blends seamlessly with neighboring status bar badges and buttons. | Look at bottom status bar with an active journal streak; verify streak icon and text share identical palette to other chips. |
| **Theme Optimization (Style & Mood Popovers)** | `src/components/NoteCard/NoteStylePicker.tsx`, `src/components/NoteCard/NoteHeader.tsx` | Card Style & Typography popover, Mood popover | Replaced card-specific paper-theme container coloring with universal application light/dark tokens (`bg-white dark:bg-slate-900 border-slate-200 dark:border-slate-800 text-slate-900 dark:text-slate-100 shadow-xl`); aligned typography labels, Select trigger, and options. | Popover headers, close buttons, section labels, and dropdowns maintain crisp, high-contrast readability without color inversions across both Light and Dark app themes regardless of individual note paper styles. | Open style picker on light note in dark mode; verify header text "Card Style & Typography", font dropdown, and segmented controls have crisp contrast and dark surfaces. |
| **Canvas Image Cards & Desktop Polish** | `src/components/NoteCard/ImageNoteCard.tsx`, `src/components/NoteCard/NoteDecorations.tsx`, `src/components/NoteCard/NoteStylePicker.tsx`, `src/components/Modals/ImageLightboxModal.tsx`, `src/components/NoteContextMenu.tsx`, `src/components/BatchActionBar.tsx`, `src/App.tsx`, `src/components/NoteCard/index.tsx`, `src/types/index.ts`, `src/schemas/backupSchema.ts` | Image card action buttons, 60fps direct DOM drag, photo lock protection screen, merge disabling, slash command exclusion | Implemented complete passcode lock protection screen for `ImageNoteCard` with lock/unlock action buttons and hidden image payload when locked; removed Paper Theme section from image card right-click context menu and style picker; hidden and disabled AI Merge in `BatchActionBar` and `App.tsx` whenever any selected note is an image note; verified slash commands are strictly excluded from image notes (caption is plain input); restructured image frame styles to `Polaroid`, `Photo Print`, `Frameless`; 60 FPS hardware-accelerated direct DOM dragging; tape-only text notes. | Locking an image note completely shields the photo and caption behind a passcode-protected lock screen with one-click unlock; right-clicking an image card displays relevant photo actions without paper themes; selecting image notes hides AI Merge action; slash commands do not trigger on photos. | Select image note + text note -> verify Merge button is hidden in batch dock; select image note -> click Lock Access -> verify lock screen; type in photo caption -> verify no slash command popup. |
| **Performance Optimization (Phase 1)** | `src/components/InfiniteCanvas.tsx`, `src/components/NoteCard/NoteHeader.tsx`, `src/index.css` | Canvas drag panning, 2D HTML5 Minimap, NoteCard header responsiveness, Card layout containment | 1. Wrapped mouse drag panning in `requestAnimationFrame` with pending coordinate ref and immediate mouseup flush; 2. Decoupled `MinimapCanvas` into stacked 2-layer background notes canvas (cached) and foreground viewport canvas ($O(1)$ rectangle draw without note iterations on pan/zoom); 3. Replaced per-card runtime `ResizeObserver` in `NoteHeader` with direct prop calculation `(note.width || DEFAULT_NOTE_WIDTH) < 285`; 4. Added `contain: layout;` to `.note-card` in `index.css`. | 35%–50% CPU reduction during continuous canvas mouse panning; zero note-iteration loop execution on high-frequency pan frames; 100% elimination of ResizeObserver GC overhead; zero popover clipping. | Pan canvas continuously at 60+ FPS; verify smooth tracking without coordinate drift; pan/zoom canvas and verify minimap viewport tracks camera; resize note card to 280px and verify compact menu toggles cleanly. |
| **Custom Washi Tape Assets & Centralized Constants** | `src/assets/washi-tapes/*`, `src/constants/washiTapes.ts`, `src/components/NoteCard/NoteStylePicker.tsx`, `src/components/NoteCard/NoteDecorations.tsx`, `src/hooks/useNotesManager.ts`, `src/types/index.ts`, `src/schemas/backupSchema.ts` | Washi tape vector assets, NoteStylePicker decoration grid, NoteDecorations rendering, random pin generator | Extracted 14 custom vector SVG washi tapes from `washi tapes.zip` into `src/assets/washi-tapes/`; centralized tape/pin definitions into `WASHI_TAPES` and `PUSHPIN_OPTIONS` constants with alias mapping; updated `NoteStylePicker` to dynamically render swatches from constant; rendered crisp vector tapes in `NoteDecorations`. | Text and image note style pickers dynamically list all 14 washi tapes + pushpins from a single constant; notes render vector torn deckle washi tape strips at 60 FPS. | Open style picker on text note and image note $\rightarrow$ select each of the 14 washi tape swatches $\rightarrow$ verify crisp vector rendering on note card. |
| **Typography Scaling (Title & Save Info)** | `src/components/NoteCard/NoteHeader.tsx`, `src/components/StatusBar.tsx` | Note card title input/h3, last updated date subheader, status bar save indicator | Increased note title size to `text-lg sm:text-xl` (18px/20px), last updated date subheader to `text-sm` (14px), and status bar save indicator to `text-xs` (12px) with reserved width. | Note title is prominently bold and clear; last updated subheader and status bar save indicator are easily readable with balanced visual hierarchy. | View note on canvas; verify note title and Last Updated timestamp render crisply at increased font sizes. |
| **Glossy Tan Washi Tape Variant** | `src/assets/washi-tapes/washi-tape-15-glossy-tan.svg`, `src/constants/washiTapes.ts`, `src/types/index.ts` | Washi tape asset catalog & NoteStylePicker | Renamed `washi-tape-style1-flat-glossy.svg` to `washi-tape-15-glossy-tan.svg` in `src/assets/washi-tapes/`; registered `tape-15-glossy-tan` (`Glossy Tan`, `#EAC98B`) into `WASHI_TAPES` constant with alias mapping and type safety. | Glossy Tan washi tape is immediately available in both text note and image note style pickers; cards render the glossy tan torn deckle strip. | Open note style picker -> verify "Glossy" swatch renders -> select it -> verify card displays the glossy tan tape. |
| **Classic Tapes Restoration & Active Tape Pruning** | `src/constants/washiTapes.ts`, `src/components/NoteCard/NoteDecorations.tsx`, `src/components/NoteCard/NoteStylePicker.tsx` | Classic translucent tapes (`tape-teal`, `tape-pink`, `tape-beige`, `tape-yellow`), active catalog pruning | Restored classic translucent frosted CSS washi tapes (`Classic Teal`, `Classic Pink`, `Classic Beige`, `Classic Yellow`) into the active `WASHI_TAPES` constant; removed `tape-8`, `tape-10`, `tape-11`, and `tape-13` from active selection list while retaining fallback rendering for backwards compatibility. | Style pickers feature classic translucent tapes alongside chosen vector washi tapes; classic tapes render with realistic frosted translucent fill and jagged CSS clip-path. | Open note style picker -> select "Classic Teal" / "Classic Pink" -> verify frosted translucent tape renders on card. |
| **Native OS Image Drop & Modular Rust Processing** | `src-tauri/src/*` (`commands/*`, `models/*`, `utils/*`, `lib.rs`), `src/hooks/useNativeFileDrop.ts`, `src/App.tsx` | Canvas OS file drag-and-drop, image note creation at cursor coordinates | Architected modular Rust backend (`commands/image.rs`, `commands/export.rs`, `models/image.rs`, `utils/mime.rs`, `utils/path.rs`) with `read_image_files` command; subscribed to Tauri native `onDragDropEvent` in `useNativeFileDrop.ts` with canvas coordinate transformation; pinned photo note created right under cursor. | Dropping any image file (`.png`, `.jpg`, `.jpeg`, `.webp`, `.gif`, `.svg`, `.bmp`, `.avif`, `.ico`, `.tiff`) from native OS file manager/desktop onto the window creates a photo note pinned precisely at the cursor location. | Drag an image file from desktop/file manager and drop anywhere on canvas; verify photo note card is created at cursor with notification. |
| **Hotfix (Single Image Drop Note Duplication)** | `src/hooks/useNativeFileDrop.ts`, `src/components/InfiniteCanvas.tsx` | Native drop event listeners & DOM drop bridge | Removed redundant secondary window event listener `listen('tauri://drag-drop')` running simultaneously with `webview.onDragDropEvent`; added drop coordinate/path fingerprint deduplication guard (<800ms); ensured DOM `onDrop` skips processing in Tauri desktop environment. | Dropping an image file creates exactly ONE photo note card positioned under the cursor without duplicate ghost cards. | Drag single image onto canvas; verify exactly 1 card is created at cursor. |
| **Long-Distance Cut & Relocate (`Ctrl+X` $\rightarrow$ `Ctrl+Shift+V`)** | `src-tauri/src/*` (`commands/clipboard.rs`, `models/clipboard.rs`, `lib.rs`), `src/hooks/useNoteSelection.ts`, `src/App.tsx`, `src/components/InfiniteCanvas.tsx`, `src/components/NoteCard/index.tsx`, `src/components/NoteCard/ImageNoteCard.tsx`, `src/components/NoteContextMenu.tsx`, `src/constants/shortcuts.ts` | Keyboard shortcuts (`Ctrl+X`, `Ctrl+Shift+V`, `Ctrl+V`, `Esc`), live cursor tracking, semi-transparent dashed ghosting, native Rust spatial relocation command, right-click context menu | Integrated `Ctrl+X` to cut notes into relocation clipboard with `opacity-40 ring-2 ring-indigo-500 border-dashed` styling; `Ctrl+Shift+V` / `Ctrl+V` triggers `relocate_notes` (modular Rust command) mapping bounding centers to cursor world coordinates; `Escape` clears cut state; added Cut/Paste items to context menu & updated shortcuts cheatsheet. | Pressing `Ctrl+X` on selected notes stages them for movement with a stylish dashed ghost outline; navigating across the infinite canvas and pressing `Ctrl+Shift+V` relocates them to the mouse cursor instantly while preserving multi-note spatial layout. | Select 1 or 3 notes $\rightarrow$ press `Ctrl+X` (verify dashed outline) $\rightarrow$ pan canvas $\rightarrow$ press `Ctrl+Shift+V` (verify notes teleport to cursor) $\rightarrow$ press `Ctrl+Z` (verify undo). |
| **Hotfix (Cut-Paste Immediate Teleport & Modal Suppression)** | `src/App.tsx`, `src/hooks/useNoteSelection.ts`, `src/components/NoteCard/index.tsx`, `src/components/HiddenClipboardListener.tsx`, `src/components/Modals/AppModals.tsx` | HiddenClipboardListener coordination, instant cut deselect, 0ms teleportation, external text paste preservation | 1. Wired `hasCutNotes` and `onPasteRelocateNotes` to `HiddenClipboardListener` to cleanly execute relocation on `Ctrl+V` and stop capture-phase clipboard reads from triggering the paste modal; 2. Cleared `selectedNoteIds` immediately on `Ctrl+X` so cut ghost styling shows without manual click-away; 3. Removed `transform` from CSS transition in `NoteCard/index.tsx` so cards teleport instantly without dragging across the screen; 4. External clipboard text and image pastes continue to open modal or create photo cards when no notes are cut. | Cutting a note and pressing `Ctrl+V` instantly relocates the note without opening any modal; copying external text from outside the app and pressing `Ctrl+V` opens the "Create Note from Clipboard" modal as intended. | Cut note -> `Ctrl+V` (note moves with 0 modal); Copy text from browser -> `Ctrl+V` (paste modal opens). |
| **StatusBar Cut Indicator & Universal Escape Handling** | `src/components/StatusBar.tsx`, `src/hooks/useNoteSelection.ts`, `src/components/HiddenClipboardListener.tsx`, `src/components/NoteCard/index.tsx`, `src/App.tsx` | Live cut status bar text with Esc button, universal Escape listener, NoteCard React.memo isCut comparator | 1. Added live indicator in the bottom `StatusBar` matching standard status bar text color/typography (`text-slate-600 dark:text-slate-400 font-sans text-[11px]`) without background container (`X notes cut (Press Ctrl+V to place) [Esc]`) with interactive Esc button; 2. Moved `Escape` handling to the top of `handleKeyDown` (and in capture-phase `HiddenClipboardListener`) to reliably cancel cut state; 3. Added `prevProps.isCut === nextProps.isCut` to `NoteCard`'s `React.memo` comparator so cards instantly re-render and remove the dashed outline/dimming when cut is cancelled. | When notes are cut, the status bar displays clean inline text matching neighboring statistics; pressing `Escape` at any time cancels the cut state and the note card immediately reverts to its normal 100% opacity state. | Select note -> press `Ctrl+X` (status bar shows cut indicator without background, note dims) -> press `Escape` (status bar clears, note returns to normal). |
| **Smart Group Cut Prevention** | `src/App.tsx`, `src/components/NoteContextMenu.tsx` | Group cut guard & context menu item state | 1. In `handleCutSelectedNotes`, added check for `groupId` on target notes: blocks cutting grouped notes and displays notification (`"X note(s) belong to a group. Ungroup first (Ctrl+Shift+G) to relocate."`); 2. In `NoteContextMenu`, dynamically labeled Cut item as `"Cut Note(s) (Ungroup first)"` and disabled it whenever any selected note belongs to a group. | Grouped notes cannot be accidentally cut out of group containers; context menu and keyboard shortcut inform the user with actionable instructions. | Select grouped note -> press `Ctrl+X` (blocked with notice) -> right click (Cut is disabled with "Ungroup first"). |
| **GroupFrame Header Typography & UI Input Primitive** | `src/components/GroupFrame.tsx` | Group title badge, typography, and rename editor | Upgraded Group Header badge with larger typography (`text-sm font-semibold tracking-tight`), 16px Hugeicons `Layers01Icon` (`size="sm"`), `size="sm"` note count `Badge`, refined glassmorphism (`px-3 py-1.5 rounded-sm`), and replaced raw `<input>` with the standardized `@ui` `Input` primitive with `autoFocus`, `Enter` save, and `Escape` discard. | Group headers render with modern desktop typography, high contrast, clean hover interactions, and standard UI primitives for renaming. | Create group -> verify group header badge has larger font and clean styling -> click to rename -> edit with Input primitive -> press Enter to save. |
| **Hotfix (Cut Notification & Image Lightbox Event Isolation)** | `src/App.tsx`, `src/hooks/useNoteSelection.ts`, `src/components/ui/Dialog.tsx`, `src/components/InfiniteCanvas.tsx`, `src/components/Modals/ImageLightboxModal.tsx` | Escape key cut notification suppression, Dialog synthetic event isolation, Canvas dialog click guards, ImageLightboxModal smooth drag-to-pan & zoom controls | 1. In `App.tsx` and `useNoteSelection.ts`, guarded `handleCancelCutNotes` and `onCancelCutNotes` so "Cut Cancelled" notification ONLY fires when notes were actually in cut relocation state; 2. In `Dialog.tsx`, stopped propagation for all mouse, wheel, pointer, and double-click events across the `<dialog>` container to prevent React portal events leaking to canvas; 3. In `InfiniteCanvas.tsx`, added defensive guards against clicks originating in dialogs/modals; 4. In `ImageLightboxModal.tsx`, implemented interactive drag-to-pan, mouse wheel zooming (0.5x–4x), double-click zoom toggling (1x/2.25x), keyboard shortcuts (+/-/0/R/Esc), rotate left/right buttons, and zoom percentage indicator. | Pressing `Escape` anywhere in the app never fires a false "Cut Cancelled" notification unless notes were actively cut; double clicking, scrolling, or dragging inside the Image Preview modal smoothly pans/zooms the photo without creating background notes or moving the canvas underneath. | 1. Press `Escape` without cut notes -> verify 0 notification; 2. Double-click image note to open lightbox -> double-click preview image, scroll wheel, drag to pan -> verify smooth lightbox pan/zoom and 0 background note creation/canvas movement. |
| **Delete Confirmation Modal Flow (Note Header & Single Deletions)** | `src/App.tsx`, `src/components/Modals/AppModals.tsx`, `src/components/__tests__/DeleteConfirmationModal.test.tsx` | Note header delete button, Image card delete button, Notes sidebar delete button, Delete confirmation dialog | Routed `handleDeleteProtectedNote` through `requestDeleteNotes([noteId])` in `App.tsx`; bound `noteTitles` list preview in `AppModals.tsx` to display targeted note title and count cleanly in `DeleteConfirmationModal`. | Clicking the delete icon on any note card header (or inside overflow menu, image cards, and sidebar) opens the confirmation modal with note title preview before deletion; confirming executes deletion while canceling preserves the note. | Click the delete icon on a note header -> verify `DeleteConfirmationModal` opens with note title -> click Cancel (note preserved) -> click Delete (note deleted). |
| **Rust Native Migration (Phase 0 & Checkpoint 0)** | `src-tauri/src/*` (`models/*`, `infrastructure/os/*`, `domain/note/*`, `lib.rs`) | Hexagonal domain models, AppPaths platform resolver, single-instance mutex focus, repository contracts | Created typed Serde camelCase `Note` DTOs, cross-platform AppData directory resolver (`$XDG_DATA_HOME`, `%APPDATA%`, `Application Support`), single-instance focus handling, `StorageError` enum, and `NoteRepository` trait with `MockNoteRepository`. | Application launches with single instance focus protection, platform-aware paths, and tested hexagonal domain contracts. | Run `cargo test --lib`; verify 21 unit tests pass with 0 errors. |
| **Rust Native Migration (Phase 1 & Checkpoint 1)** | `src-tauri/src/*` (`infrastructure/sqlite/*`, `commands/storage.rs`), `src/canvas/*` (`spatialIndex.ts`, `viewport.ts`), `src/lib/rustStorage.ts`, `src/components/StatusBar.tsx`, `src/components/InfiniteCanvas.tsx` | Native SQLite WAL database engine, versioned migrations, atomic transactions, in-memory 2D R-Tree spatial virtualizer, StatusBar engine indicator | Built `SqliteNoteRepository` with WAL mode and `PRAGMA synchronous = NORMAL;`, typed Tauri storage commands, TS bridge `rustStorage.ts` with automatic IndexedDB migration, in-memory R-Tree spatial index with dynamic frustum overscan culling $< 200$ DOM NoteCards, and updated StatusBar to display "Native SQLite". | High-performance native SQLite storage engine with zero data loss on instant close, automatic first-boot IndexedDB migration, and 60 FPS canvas spatial virtualization across 10,000+ notes. | Run `cargo test` (21 passed) and `npm test` (134 passed, 30 suites); verify StatusBar displays "Native SQLite". |
| **Rust Native Migration (Phase 2 & Checkpoint 2)** | `src-tauri/src/*` (`infrastructure/filesystem/asset_store.rs`, `domain/asset/*`, `commands/assets.rs`, `lib.rs`), `src/lib/rustAssets.ts`, `src/lib/__tests__/rustAssets.test.ts`, `src/lib/index.ts` | Content-Addressable Asset Store, SHA-256 deduplication, atomic staging pipeline, custom `diarynote-asset://` URI scheme protocol, WebP thumbnail generator | Architected `AssetStore` with strict 64-hex SHA-256 path traversal guards, atomic staging pipeline in temp dir, automatic WebP thumbnail generation via `image` crate, Tauri v2 custom URI scheme `diarynote-asset://<hash>`, SQLite `assets` catalog registration, and typed TypeScript bridge `rustAssets.ts`. | Dropped and imported media files are stored deduplicated under SHA-256 hashes with fast cached URI streaming and automatic thumbnails without IPC memory bottlenecks. | Run `cargo test` (24 passed) and `npm test` (138 passed, 31 suites); verify hash validation, deduplication, and `diarynote-asset://` URI generation. |
| **Rust Native Migration (Phase 3 & Checkpoint 3)** | `src-tauri/src/*` (`infrastructure/crypto/*`, `domain/vault/*`, `commands/vault.rs`, `lib.rs`), `src/lib/rustVault.ts` | Cryptographic Hardware Vault, AES-256-GCM envelope, PBKDF2/Argon2id, memory zeroization, exponential backoff rate limiter | Implemented envelope encryption at rest, secure memory zeroization on drop (`ZeroizeOnDrop`), and native brute-force rate-limiting. | Locked note contents remain completely ciphertext on disk and zeroed from memory when locked; unlock attempts are rate-limited with exponential backoff. | Run `cargo test --lib domain::vault::service infrastructure::crypto`; verify 28 passed tests. |
| **Rust Native Migration (Phase 4 & Checkpoint 4)** | `src-tauri/src/*` (`infrastructure/sqlite/fts.rs`, `domain/graph/*`, `commands/search.rs`, `commands/graph.rs`, `lib.rs`), `src/lib/rustSearch.ts`, `src/lib/rustGraph.ts` | Dual-tier FTS5 lexical search (disk trigram + memory FTS5) & Markdown link graph parser | Built high-performance trigram FTS5 search with locked note isolation, pulldown-cmark CommonMark AST parser for @[Title](id) mentions, and backlink graph resolution. | Sub-millisecond search across all languages/scripts with zero locked note leaks; bidirectional note connections resolved instantly. | Run `cargo test --lib infrastructure::sqlite::fts domain::graph`; verify all search and link parser tests pass. |
| **Rust Native Migration (Phase 5 & Checkpoint 5)** | `src-tauri/src/*` (`infrastructure/filesystem/backup.rs`, `commands/backup.rs`, `models/backup.rs`, `lib.rs`), `src/lib/rustBackup.ts`, `src/lib/__tests__/rustBackup.test.ts`, `src/lib/storage.ts` | WAL-safe Online Backup Engine, SQLite Online Backup API (`rusqlite::backup`), streaming `.diarynote` ZIP container with versioned `manifest.json`, staged restore | Built point-in-time database snapshotting using SQLite Online Backup API (`rusqlite::backup::Backup`) without detached WAL corruption; bundles snapshot with `manifest.json` and CAS image assets into `.diarynote` archive; supports `keep-both`, `overwrite`, and `skip` duplicate resolution modes. | Full vault backup produces 100% valid snapshots during active concurrent writes; restoring archives recovers all notes, settings, and original media assets without ID collisions. | Run `cargo test --lib infrastructure::filesystem::backup commands::backup` and `npm test src/lib/__tests__/rustBackup.test.ts`; all tests pass. |
| **Rust Native Migration (Phase 6 & Checkpoint 6)** | `src-tauri/src/*` (`infrastructure/network/ai_client.rs`, `domain/ai/*`, `commands/ai.rs`, `lib.rs`), `src/services/ai/aiMergeService.ts` | Secure Streaming AI Gateway, `reqwest` HTTP/2 client with SSE token streaming, backend credential protection | Implemented streaming AI gateway supporting Gemini, OpenAI, OpenRouter, and Custom providers; tokens stream via Tauri events (`ai:stream-chunk`); credentials remain isolated in native backend without exposure in webview devtools. | Real-time token streaming into notes and auto-tagging without blocking the UI thread; zero API keys or third-party AI network requests visible in WebView DevTools. | Run `cargo test --lib infrastructure::network::ai_client commands::ai` and `npm test src/services/__tests__/aiMergeService.test.ts`. |
| **Rust Native Migration (Phase 7 & Checkpoint 7)** | Universal Codebase & Build Configs | Production build verification, static analysis, clippy cleanup, and release readiness | Fixed all compiler/clippy warnings; ran full suite of unit/integration tests; verified production bundle with code splitting and vendor isolation (`vendor-icons`, `vendor-db`, `vendor-motion`, `vendor-markdown`, `vendor-react`). | Application builds cleanly in release mode with 0 lint errors, 0 clippy warnings, 152/152 passing frontend tests, and 38/38 passing native Rust tests. | Run `cargo test --all`, `cargo clippy --all-targets`, `npm run lint`, and `npm run build`. |
| **Backend-Frontend Full Connectivity Audit** | `src/hooks/useCanvasTransform.ts`, `src/App.tsx`, `src/utils/security.ts` | Canvas transform persistence, app settings persistence, staged import saving, security passcode hashing/verification | Standardized `useCanvasTransform` and `App.tsx` to import `saveCanvasTransformToDB`, `saveAppSettingsToDB`, and `saveDirtyNotesToDB` from `rustStorage.ts`; forwarded `hashSecurityInput` and `verifySecurityInput` in `security.ts` to `rustVault.ts`. | All settings, canvas transforms, imported notes, and master passcode hashing/verification execute directly through native Tauri Rust commands (`save_canvas_transform`, `save_app_settings`, `save_notes_batch`, `vault_hash_security_input`, `vault_verify_security_input`) when in desktop mode. | Verify `npm test` (152/152 passed), `npm run lint` (0 errors), and `cargo check`. |
| **Architecture Alignment & Cleanup** | `src/components/Modals/SearchModal.tsx`, `src/components/Modals/AppModals.tsx`, `src/types.ts`, `src/lib/sqliteStorage.ts`, `src/workers/*`, `src/components/NoteCard.tsx` | SearchModal FTS integration, vault FTS sync on lock/unlock, removed dead/redundant shim files | Connected `SearchModal` to native `searchNotesFts` in Tauri mode; synchronized `indexVaultNotesFts`/`clearVaultFtsIndex` on unlock/lock; purged redundant re-export shims (`src/types.ts`, `src/lib/sqliteStorage.ts`, `src/components/NoteCard.tsx`) and unneeded web worker prototype (`src/workers/search.worker.ts`). | Search provides instant sub-millisecond SQLite FTS5 results with trigram tokenization; zero dead shim files or unneeded prototype workers remain in the project. | Run `npm test` (152/152 passed), `npm run lint` (0 errors), `cargo check`. |
| **Complete Legacy DB & Migration Code Purge** | `src/lib/indexedDbStorage.ts` (deleted), `src/lib/rustStorage.ts`, `src/lib/index.ts`, `src/components/ErrorBoundary.tsx`, `package.json`, `vite.config.ts`, `src-tauri/src/infrastructure/filesystem/backup.rs` | Storage layer, emergency crash recovery, build configuration, native backup inspection | Completely removed Dexie, `fake-indexeddb`, `indexedDbStorage.ts`, and first-boot migration checks; streamlined `rustStorage.ts` to pure native SQLite with an in-memory mock for headless test runners; uninstalled `dexie` package; removed `LegacyBackup` and legacy JSON backup fallback from Rust backend. | 100% pure native SQLite storage architecture; zero dead Dexie/IndexedDB code, zero migration overhead on startup, and clean production bundles. | Run `npm test` (150/150 passed), `npm run lint` (0 errors), `cargo check` (0 warnings), `cargo test --all` (38/38 passed), and `npm run build`. |
| **Startup Images, Pin Defaults & Smart Notifications** | `src-tauri/tauri.conf.json`, `src-tauri/src/lib.rs`, `src/components/NoteCard/ImageNoteCard.tsx`, `src/hooks/useNotesManager.ts`, `src/App.tsx`, `src/utils/notifications.ts`, `src/components/BatchActionBar.tsx`, `src/components/Modals/SecurityModal.tsx`, `src/components/Modals/AppModals.tsx`, `src/components/NoteCard/index.tsx` | Canvas image rendering, CSP config, URI scheme handler, default pin state, and application-wide notification orchestration | Configured CSP `img-src` with `diarynote-asset:` / `asset:`; added CORS headers and resilient hash extraction in Rust; changed `loading="lazy"` to `loading="eager"` with error fallbacks; changed journal and photo default to `isPinned: false`; implemented smart deduplication/permission caching in `notifications.ts`; wired native notifications across pin, align, group, duplicate, passcode setup, and unlock workflows. | Images load immediately on startup in canvas without CSP blocks or lazy rendering delays; notes are unpinned by default; desktop notifications trigger cleanly without spam. | Run `npm test` (150/150 passed), `npm run lint` (0 errors), `cargo check` (0 warnings), `cargo test --all` (38/38 passed), and `npm run build`. |
| **Real-Time AI Streaming & Database Operations Modal** | `src-tauri/src/*` (`commands/storage.rs`, `models/app_state.rs`, `lib.rs`), `src/components/Modals/DatabaseOperationsModal.tsx` *(New)*, `src/services/ai/aiMergeService.ts`, `src/App.tsx`, `src/components/StatusBar.tsx`, `src/components/CanvasControls.tsx`, `src/components/Modals/CanvasSettingsModal.tsx`, `src/lib/rustStorage.ts` | Immediate AI synthesized note placement with 60 FPS token streaming, dedicated Database & Storage management modal, Live SQLite metrics, Vacuum/Defrag, and full vault snapshots | 1. Placed synthesized AI note immediately on canvas upon merge click with live token streaming via `ai:stream-chunk` Tauri events; 2. Created dedicated Database & Storage Operations Modal conforming to unified `@ui` tokens; 3. Bound StatusBar "Native SQLite" badge click to open the modal instead of running an un-prompted direct backup; 4. Added live SQLite file size, WAL size, page count, integrity check, on-demand Vacuum optimization, and full `.diarynote` snapshots with destination display. | Clicking "Merge with AI" creates the note card immediately at the center of selected notes and streams incoming text in real-time; clicking "Native SQLite" opens an informative, interactive database operations center. | 1. Select 2 notes -> click Merge with AI -> verify note card appears immediately and streams tokens; 2. Click "Native SQLite" in status bar -> verify Database Operations modal opens with live metrics, vacuum, and backup controls. |
| **Complete Emoji Purge & AI Prompt Hardening** | `src/App.tsx`, `src/services/authPolicyService.ts`, `src/utils/markdownMention.ts`, `src/constants/aiPrompts.ts` | Merge note placeholder, locked note labels, mentions, and AI synthesis prompt rules | 1. Removed `✨` emoji from AI merge placeholder; 2. Removed `🔒` emoji from redacted locked note titles and markdown mentions; 3. Added strict negative prompt instruction to `aiPrompts.ts` prohibiting emojis and decorative icons in AI generated titles and body copy. | Merge note initialization and locked note indicators render clean typography without emoji artifacts; AI synthesized notes never output emoji characters. | Merge notes with AI -> verify note placeholder and generated content contain zero emojis. |
| **Native Rust Migration & Boot Speed Optimization** | `src-tauri/src/*` (`commands/layout.rs` *(New)*, `commands/export.rs`, `commands/image.rs`, `infrastructure/sqlite/connection.rs`, `infrastructure/sqlite/repository.rs`), `src/components/BatchActionBar.tsx`, `src/components/Modals/SearchModal.tsx`, `src/components/Modals/AppModals.tsx`, `src/components/CanvasControls.tsx`, `src/utils/layoutUtils.ts`, `src/App.tsx`, `src/lib/rustStorage.ts` | Single-note native file exporter, mass note layout and grid alignment engine, image dimensions parser, SQLite pragmas & N+1 batch query removal, FTS5 highlight rendering, deferred update checks, and idle modal chunk prefetching | 1. Implemented native `export_note_to_file` in Rust writing directly to disk with path notifications; 2. Created high-performance `compute_batch_layout` command in Rust for instant sub-millisecond alignment and distribution; 3. Added `aspect_ratio`, `width`, `height` extraction in Rust `read_image_files`; 4. Configured SQLite memory-mapped I/O (256MB) and 64MB cache pragmas; 5. Replaced $N+1$ tag query in `load_all` with 2 batch queries; 6. Rendered pre-highlighted FTS5 snippets directly in `SearchModal`; 7. Implemented `prefetchAppModals` via `requestIdleCallback` for instant 0ms modal opening; 8. Deferred GitHub update checks by 3s after mount. | Mass alignments compute in microseconds; single note exports write cleanly to disk; initial note boot is blazing fast (<20ms); all modals open with 0ms visual delay. | 1. Export a single note as `.md` -> verify native save and path toast; 2. Select 5+ notes -> click Align Left / Grid Pack -> verify instant alignment; 3. Press Ctrl+K -> search for text -> verify native FTS highlighted snippets; 4. Open preferences/modals -> verify instant opening. |
| **Pure Rust GPUI Migration (Phase 3)** | `crates/ui/src/*` (`tokens/*`, `primitives/*`, `components/*`, `views/*`, `lib.rs`, `Cargo.toml`) | Pure Rust GPUI Design System, Standard Tokens, Atomic Primitives, Composite Components, Declarative Views | Wired `gpui` 0.2.2 dependency directly into workspace and `crates/ui`; implemented typed `gpui::Rgba`/`gpui::Hsla` color conversions, `CornerRadii.to_gpui_corners()`, `BoxShadow` to `gpui::BoxShadow`, 15 atomic UI primitives implementing `gpui::IntoElement` (`Button`, `IconButton`, `Input`, `Textarea`, `Checkbox`, `Switch`, `Badge`, `Kbd`, `Dialog`, `Menu`, `Tabs`, `SegmentedControl`, `Select`, `Tooltip`, `Icon`), 8 composite components (`StatusBar`, `BatchActionBar`, `NoteHeader`, `NoteToolbar`, `StylePicker`, `SlashMenu`, `MentionMenu`, `Minimap`), and 4 declarative views (`InfiniteCanvasView`, `NoteCardView`, `NotesSidebarView`, `ModalsView`). | 100% pure native Rust GPU-accelerated UI foundation producing real GPUI elements (`gpui::div()`, flex layouts, hover/active styling, 4px corner radii, subtle shadows, and light/dark theme adaptation). | Run `cargo test -p ui` (35/35 passed), `cargo test --workspace` (49/49 passed), `cargo clippy --workspace --all-targets`, `cargo fmt --check`, and `npm run lint`. |

---


## 4. Core Architectural Invariants for Agents

All agents working on DiaryNote must adhere to these standing principles:

1. **Desktop Native & Offline First:**
   - DiaryNote is a desktop application (Tauri + Rust + React). Do not introduce assumptions of remote web servers, cloud sync, or hosted SaaS infrastructure.
   - All network interactions (updates, AI endpoints) must be strictly user-configurable and default to privacy-preserving boundaries.

2. **Zero-Loss Persistence Protocol:**
   - Never bypass the note repository layer with direct React state overrides.
   - A note is only considered saved after IndexedDB storage confirmation resolves.

3. **Zero-Knowledge Security & Privacy:**
   - Locked notes must be encrypted at rest using Argon2id + AES-256-GCM. Plaintext locked content must never touch persistent storage or unauthenticated memory caches.
   - Exclude locked notes from exports, clipboard copies, AI prompts, and graph indexes unless explicitly authenticated.

4. **CPU & Rendering Performance:**
   - Never call `getBoundingClientRect()`, `offsetWidth`, or `offsetHeight` inside mousemove or touchmove loops.
   - Decouple note metadata from markdown bodies. Heavy search and cryptography operations must run in Web Workers or native Rust commands.

5. **Quality Verification Before Completion:**
   - Every task must pass:
     ```bash
     npm run lint
     npm run build
     cargo check --manifest-path src-tauri/Cargo.toml
     ```
   - Automated tests (`npm test`) must pass with zero errors before any task is marked done.

---

## 5. UI Regression Troubleshooting Guide

If a UI component stops functioning, misaligns, or clips after a task:
1. **Check the Registry Table Above:** Find the file in the registry and review what props, state, or DOM structure were altered in that phase.
2. **Inspect Event Delegation:** Ensure Pointer Events (`onPointerDown`, `setPointerCapture`) are not being blocked by overlapping containers or missing `touch-action: none`.
3. **Check Portal Mounting:** Ensure popovers and floating menus (e.g., Theme/Align menus) use `createPortal(..., document.body)` to prevent container overflow clipping.
4. **Verify CSS Classes:** Verify Tailwind CSS utility classes conform to Tailwind v4 syntax without deprecated arbitrary values.
