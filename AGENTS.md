# AGENTS.md — Agent Working Rules & UI Component Registry

**Document Version:** 1.0.0  
**Target Application:** DiaryNote Desktop (Tauri + Rust + React + TypeScript)  
**Applicability:** Mandatory for all AI agents, subagents, and automated assistants operating on this codebase.

---

## 1. Mandatory Agent Rule: UI Component Modification Registry

> [!IMPORTANT]
> **RULE #1 (UI CHANGE MANIFEST):**
> Whenever an agent modifies, refactors, creates, or deletes **any UI component, modal, hook affecting rendering, or stylesheet** (`src/components/**`, `src/hooks/**`, `src/index.css`), the agent **MUST immediately update the UI Component Modification Registry below in this document (`AGENTS.md`)**.
>
> This enables instant identification of visual regressions, broken event handlers, or UI mismatches across phases.

### Active UI Component Modification Registry

| Phase & Task ID | File Path | UI Elements Affected | Nature of Change | Expected Visual & Functional Behavior | Regression Verification Checklist |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Notification Noise Reduction (High-Priority Only)** | `src/App.tsx`, `src/components/Modals/AppModals.tsx`, `src/components/BatchActionBar.tsx`, `src/components/NoteCard/index.tsx`, `src/components/NoteCard/ImageNoteCard.tsx`, `src/components/Modals/DatabaseOperationsModal.tsx` | Native desktop toasts only — no visual component changes; removed 22 low-priority notification call sites (cover/uncover, pin/unpin, theme applied, group/ungroup/duplicate, note/photo created, cut/placed/cancelled, AI settings saved, AI merge started, generating tags, auto-tag complete, path copied, note locked/unlocked toggles) | Deleted the `sendNativeAppNotification` invocations and their now-dead locals; removed orphaned import in `ImageNoteCard`. Retained all high-priority notifications: errors/failures (Import Failed/Error, AI Merge Failed, Auto Tag Failed), security & auth events (passcode set/change/reset/disable/recovery/vault/session), guards explaining blocked actions (Note Protected, Delete/Export Restricted, AI Key Required, Cannot Cut Grouped, Cannot Merge Photos, Already Merged), async completions (AI Note Merged Successfully), file receipts with paths (Export Successful, Backup Created/Downloaded, JSON Backup Exported, Database Optimized, Import Successful), and destructive receipts (Note/Notes Deleted). | Routine state changes complete silently (UI already reflects them visually); OS toasts fire only for security events, errors, blocked actions, long-running async results, and file operation receipts. No handler logic changed — only notification emission removed. | 1. Toggle cover/pin/theme/group/delete-single -> verify NO toast fires.<br>2. Trigger export (.md/.json) or full backup -> verify receipt toast WITH save path still appears.<br>3. Attempt copy on a locked note / batch-delete locked notes / merge photos via AI -> verify guard toasts still appear.<br>4. Change or reset passcode -> verify security toast still appears.<br>5. Force an AI merge failure (bad key) -> verify failure toast still appears.<br>6. Run `npm run lint`, `npm test`, `npm run build` -> verify 100% pass. |
| **Note Cover Discoverability UX (Mouse Affordances + Shortcut Collapse)** | `src/components/NoteContextMenu.tsx`, `src/components/BatchActionBar.tsx`, `src/components/NoteCard/NoteStylePicker.tsx`, `src/components/Modals/AppModals.tsx`, `src/App.tsx`, `src/hooks/useNoteSelection.ts`, `src/constants/shortcuts.ts`, `src/hooks/__tests__/useNoteSelection.test.ts` | Context-menu "Cover / Remove Cover" item with dynamic label + icon + Alt+C hint (single & multi-selection), BatchActionBar "Cover / Uncover" ghost button next to Pin, Cover tab header state chip ("Covered · Alt+C to unseal" / "Not covered · Alt+C to seal", monotonic inverted chip when covered), cover shortcut collapsed from 3 aliases (Alt+C / Shift+C / Ctrl+Shift+C) to canonical Alt+C only | Added `onToggleCover` prop threading from App's `handleToggleCoverSelectedNotes` through `AppModals` into context menu and batch bar; picker chip reflects live `note.isCovered` state informationally (no on/off switch re-introduced); keydown guard now requires bare Alt+C with all other modifiers absent; shortcut docs and tests updated accordingly. | Covers can be enabled/removed entirely via mouse (right-click menu on any selection, batch bar button for multi-selections); style picker communicates current covered state and the toggle keybind without ambiguity; only Alt+C toggles covers — Shift+C and Ctrl+Shift+C no longer trigger it; no regression to connections toggle ('C'), lock (Ctrl+L), or other single-letter shortcuts. | 1. Right-click single note -> verify "Cover Note" item with Alt+C hint; click -> note seals.<br>2. Right-click covered note -> verify label flips to "Remove Cover"; click -> unseals.<br>3. Marquee-select ≥2 notes -> verify Batch bar shows "Cover"; click -> all seal with default airmail style + toast.<br>4. Click "Uncover" in batch bar on covered selection -> verify all reveal.<br>5. Open Card Style picker -> Cover & Seal tab -> verify state chip shows correct covered/not-covered wording and flips live after Alt+C.<br>6. Press Shift+C and Ctrl+Shift+C with selection -> verify NO cover toggle fires.<br>7. Press plain C -> verify connections still toggle.<br>8. Run `npm run lint`, `npm test`, `npm run build` -> verify 100% pass. |
| **Canvas Pan/Drag Performance P0 (Memo Restoration, Hysteresis Commits, Blur Removal, Cover Paint Cheapening)** | `src/App.tsx`, `src/components/InfiniteCanvas.tsx`, `src/components/NoteConnections.tsx`, `src/components/StatusBar.tsx`, `src/components/NoteCard/NoteToolbar.tsx`, `src/components/GroupFrame.tsx`, `src/constants/paperThemes.ts`, `src/constants/noteCovers.tsx`, `src/components/NoteCard/NoteCoverDecorations.tsx`, `src/components/NoteCard/index.tsx` | InfiniteCanvas memo boundary (latest-ref stable callback props), pan gesture frustum-hysteresis commits + navigation glide cancel + imperative minimap viewport rect, NoteConnections numeric `viewportBounds` comparator, backdrop-blur removal from bottom dock / minimap panel / status bar / note toolbar / group badge, Glass paper theme translucent solid, duplicate washi `NoteDecorations` removal on text notes, axis-aligned airmail SVG pattern tile (`patternTransform` removed), cover shadows `shadow-md/lg` → `shadow-sm` | Replaced 7 inline arrow props in App with latest-ref stable callbacks so `React.memo(InfiniteCanvas)` holds; replaced fixed 120ms mid-pan state commits with ~⅓-viewport hysteresis commits; added one-shot glide-cancel commit at pan mousedown; minimap FG rect now drawn imperatively inside the pan rAF via shared `drawMinimapViewport`; airmail barber-pole stripes baked into a 51×51 axis-aligned pattern tile with identical perpendicular widths and "/" direction. | Panning and dragging produce near-zero mid-gesture React commits (~8/s → 0–few); long pans never show blank culling voids; minimap viewport rectangle tracks the gesture in real time without commits; starting a pan during arrow-key navigation glide cancels it instantly with no transform fighting; dock/minimap/statusbar/toolbars render as flat opaque panels in both themes; Glass theme cards render translucent solid without frosted blur; covered vintage-airmail notes paint cheap axis-aligned diagonal borders visually identical to before; uncovered notes no longer double-paint washi tapes. | 1. Space/middle-mouse pan across >2 viewports -> verify smooth motion, no blank voids, minimap rect glued to gesture.<br>2. Trigger arrow-key navigation then immediately pan -> verify glide cancels instantly, no jitter.<br>3. Release pan -> verify final position persists after app reload.<br>4. Wheel zoom -> verify responsiveness and zoom % readout unchanged.<br>5. Drag text/checklist/image notes incl. covered notes -> verify smooth, no repaint stutter.<br>6. Verify dock/statusbar/minimap are opaque flat panels in light+dark.<br>7. Covered vintage-airmail note -> verify diagonal red/blue border direction ("/") and stripe width match the previous design.<br>8. Uncovered note with washi tape -> verify exactly one tape renders.<br>9. Run `npm run lint`, `npm test`, `npm run build`, `cargo check` -> verify 100% pass. |
| **Optimization & Flaw Remediation (Spatial Nav & Graph Engine)** | `src/hooks/useNoteSelection.ts`, `src/components/NoteConnections.tsx`, `src/utils/layoutUtils.ts`, `src/lib/rustLayout.ts`, `src-tauri/src/commands/layout.rs`, `src-tauri/src/domain/graph/service.rs` | Arrow-key navigation concurrency guard, camera auto-framing, edge-to-edge spatial ray casting, debounced connection rendering, zero-knowledge locked note graph redaction | Added `navSequenceRef` & `activeNavTargetIdRef` in `useNoteSelection` to prevent out-of-order race conditions; integrated `onNavigateToNote` for off-screen viewport auto-glide; updated Rust spatial formula with edge-to-edge projection; debounced graph calculation by 150ms in `NoteConnections`; enforced zero-knowledge locked note omission in `GraphService`. | Rapid arrow keypresses smoothly glide focus across cards without race conditions or jitter; camera auto-pans when target is off-screen; unauthenticated locked note graph edges are redacted; typing does not trigger high CPU IPC graph re-calculations. | 1. Hold ArrowRight -> verify smooth sequential movement without jitter.<br>2. Navigate to off-screen card -> verify viewport auto-centers.<br>3. Create locked note with mention -> verify connections SVG excludes locked node. |
| **Linux Middle-Mouse Pan & Paste Guard** | `src/components/InfiniteCanvas.tsx`, `src/components/HiddenClipboardListener.tsx` | Middle mouse scroll button panning, canvas mousemove tracking, clipboard paste event guard | Added `onAuxClick` prevention, stopped middle mouse propagation on mousedown/mouseup, tracked mousemove coordinates during panning, and guarded `HiddenClipboardListener` so unprompted Linux middle-click/PRIMARY selection paste events are discarded unless explicitly initiated via Ctrl+V/Cmd+V. | Clicking and dragging with middle mouse button smoothly pans the canvas with `cursor-grabbing` and releasing it never accidentally pops open the Paste Note modal. | 1. Click and drag middle mouse button on canvas -> verify smooth canvas pan.<br>2. Release middle mouse button -> verify NO paste modal appears.<br>3. Press Ctrl+V with text in clipboard -> verify paste modal opens normally. |
| **Hybrid Markdown Engine & Smart Interactions** | `src/components/NoteCard/SmartMarkdownText.tsx`, `src/components/NoteCard/index.tsx`, `src/components/NoteCard/NoteChecklist.tsx`, `src/utils/inlineMarkdownScanner.tsx`, `src/utils/noteTextEngine.ts`, `src/utils/markdownMention.ts` | Fast-path Tier 1 inline markdown scanner, smart auto-pairing, closing step-over, pair backspace, smart URL link pasting into selected text, multiline checklist paste parsing, reference-cached note lookup maps | Integrated `renderInlineMarkdown` fast-path for short strings and checklist rows bypassing heavy Remark AST parser; added auto-pair wrapping and step-over in `NoteCard` textarea; added URL link pasting over selected text in `NoteCard`; added multiline checklist paste splitting in `NoteChecklist`; added `getNoteLookupMaps` caching in `markdownMention.ts`. | Typing and editing feels instantaneous ($<8\text{ms}$); typing opening pairs wraps selection; pasting URLs over selected text automatically creates markdown links; checklist mode parses multiline pastes into individual rows without freezing or frame drops. | 1. Select text in note editor and press `[` -> verify text wrapped in `[]`.<br>2. Select text and paste `https://example.com` -> verify formatted as `[text](https://example.com)`.<br>3. Paste 3 lines of tasks into Checklist "Add task" -> verify 3 separate items created.<br>4. Run `npm test` -> verify all 38 test suites pass cleanly. |
| **Smart Popover Positioning (Slash Command & Mention)** | `src/components/NoteCard/SlashCommandMenu.tsx`, `src/components/MentionAutocomplete.tsx`, `src/utils/textareaCursor.ts`, `src/components/NoteCard/index.tsx` | Slash command menu `/` and Mention autocomplete `@` popovers | Unified cursor anchor computation; added dynamic DOM height/width measurement in `useLayoutEffect`; implemented directional vertical flipping (above/below cursor) based on available space and actual menu height; added horizontal note edge clamping. | Popovers always appear directly adjacent to the cursor line without jumping or floating in the middle of notes; when typed near the bottom of a note card, popovers flip snugly above the cursor; right and left edges never overflow the note boundary. | 1. Type `/auto` at the bottom line of a tall note -> verify popover sits snugly right above `/auto`.<br>2. Type `/` at the top of a note -> verify popover renders directly below the cursor.<br>3. Type `/` near the right edge -> verify menu does not clip outside the note.<br>4. Run `npm test` -> verify all test suites pass. |
| **Note Tab & Whitespace Indentation Persistence** | `src/components/NoteCard/BaseMarkdownRenderer.tsx`, `src/utils/noteTextEngine.ts` | Markdown preview mode paragraph indentation, tab rendering & multiple blank line preservation | Added `preserveNoteTabsAndIndentation` to prevent 4+ spaces / 1+ tabs from accidentally triggering CommonMark indented code blocks; converted leading tabs/spaces on normal lines to non-breaking whitespace; preserved multiple consecutive blank lines (3+ enters) as `&nbsp;` paragraph spacers; added `disableIndentedCodePlugin` to Remark plugins. | Single tabs (`\t`), multi-tabs (e.g. 5 tabs), and multiple blank lines (3+ enters) are persistent and preserved in view mode matching the exact physical height of edit mode with the note's normal font; paragraphs never collapse intentional whitespace; fenced code blocks (```` ``` ````) and nested markdown lists continue to render correctly. | 1. Type 1 tab before a sentence -> verify 1 tab indent in preview mode.<br>2. Type 5 tabs before a sentence -> verify 5 tabs indent with regular font.<br>3. Press Enter 3 times between paragraphs -> verify 2 visual blank lines preserved.<br>4. Run `npm test` -> verify all tests pass. |
| **Checklist Mode Regex Isolation & Zero-CPU Lockup** | `src/utils/inlineMarkdownScanner.tsx`, `src/components/NoteCard/NoteChecklist.tsx` | Inline markdown scanner regex instance isolation and recursion guard | Replaced module-level shared global `/g` regex with local instance per execution in `renderInlineMarkdown`; added `depth` recursion limit to prevent state corruption when parsing nested inline tokens (e.g., `**Borrow (`&T`)**`); wrapped `syncBackToContent` in `useCallback` and cleaned bullet marker stripping in `NoteChecklist`. | Switching any note (including notes with nested bold, code, and special symbols) to Checklist mode is instantaneous ($<5\text{ms}$) with zero memory leaks, zero CPU spikes, and zero browser freezing. | 1. Open note with nested code and bold (`**Borrow (`&T`)**`) -> click Checklist mode -> verify switches immediately without freezing.<br>2. Check CPU and RAM -> verify normal idle CPU (<2%).<br>3. Run `npm test` -> verify all 38 test suites pass. |
| **Theme-Adaptive Select Component & Unified Typography Pickers** | `src/components/ui/Select.tsx`, `src/components/NoteCard/NoteStylePicker.tsx`, `src/components/Modals/AISettingsModal.tsx`, `src/components/Modals/SecurityModal.tsx`, `src/components/NotesSidebar.tsx`, `src/components/Modals/SearchModal.tsx`, `src/components/StatusBar.tsx` | Font selection dropdown, sidebar sort selector, AI provider selector, security question selector | Fixed invalid `dark:bg-slate-850` in `Select.tsx` by transitioning to standard Tailwind `bg-slate-50/80 dark:bg-slate-800/90` with matching text/option colors; replaced native unstyled `<select>` elements in `AISettingsModal` and `SecurityModal` with unified `Select` component; cleaned up invalid `slate-850` classes in `SearchModal` and `StatusBar`. | All select dropdowns (Font selection in Card Style picker, Sidebar note sorter, AI provider, Security recovery question) adapt smoothly and cleanly to both light and dark themes with high contrast, legible typography, styled chevron arrows, and dark option backgrounds. | 1. Open Card Style & Typography on a dark note -> verify Handwriting & Font select has dark background and crisp white text.<br>2. Open Sidebar -> verify Newest Created select is dark themed.<br>3. Open AI Settings & Security Modals -> verify all selects match dark/light theme.<br>4. Run `npm test` and `npm run lint` -> verify 100% pass. |
| **Zero-Knowledge Locked Note Redaction & Search Shield** | `src/components/Modals/SearchModal.tsx`, `src/components/NotesSidebar.tsx`, `src-tauri/src/domain/search/service.rs` | Search modal note preview snippets, sidebar note snippets, tag aggregation stats, query content matching, Rust SQLite FTS search enrichment | Integrated `isNoteAuthorized` checks across frontend `SearchModal` and `NotesSidebar` to completely hide content snippets (`Passcode protected · Content hidden`), omit content-based hashtags, and block keyword matching on secret text for unauthorized locked notes; updated Rust `search_notes` and metadata retrieval in `service.rs` to select `is_locked` and redact snippets on locked notes. | Locked notes never leak plaintext snippets, hashtags, or search matches in Search Modal or Sidebar unless explicitly authenticated in session; unauthorized users only see the note's title and `Locked` badge with a secure passcode required placeholder. | 1. Create a locked note with secret keyword -> open Search Modal -> verify snippet displays "Passcode protected · Content hidden".<br>2. Search for the secret keyword -> verify locked note is NOT returned in search results.<br>3. Open Sidebar -> verify locked note displays "Passcode protected · Content hidden" and yellow Locked badge.<br>4. Run `npm test` and `cargo test` -> verify 100% pass. |
| **Password Reset, Change & Security Settings Suite + Export Notification Fix** | `src/components/Modals/SecurityModal.tsx`, `src/components/Modals/CanvasSettingsModal.tsx`, `src/components/CanvasControls.tsx`, `src/components/Modals/AppModals.tsx`, `src/App.tsx`, `src/lib/rustStorage.ts` | Dedicated Security & Lock settings tab, passcode change, recovery question password reset, recovery question updates, disable passcode, native export notifications | Added Security & Lock preferences tab in `CanvasSettingsModal`; expanded `SecurityModal` with `'set' \| 'unlock' \| 'change' \| 'reset' \| 'update_recovery' \| 'remove'` modes; connected session lock toggle; fixed Tauri IPC parameter `exportFormat` in `rustStorage.ts`; added desktop export success notifications. | Users can change or reset forgotten passcodes via security question from preferences or unlock prompt; export buttons display immediate native desktop notifications with file save path. | 1. Open Canvas Preferences -> click Security & Lock -> verify status and actions render.<br>2. Click Change Passcode -> verify changes master password.<br>3. Click Reset Passcode -> verify verifies question and resets passcode.<br>4. Click Export (.md) or Backup (.json) on a note -> verify notification popup with file path appears.<br>5. Run `npm test` and `npm run lint` -> verify 100% pass. |
| **Desktop Native External URL Redirection** | `src-tauri/src/commands/url.rs`, `src-tauri/src/lib.rs`, `src/utils/urlOpener.ts`, `src/components/NoteCard/BaseMarkdownRenderer.tsx`, `src/utils/inlineMarkdownScanner.tsx`, `src/components/UpdateAlertBanner.tsx`, `src/components/Modals/AboutModal.tsx` | External link clicks in note preview markdown, fast inline scanner, update banner, and about modal | Added native `open_external_url` Rust command delegating to platform launchers (`xdg-open` on Linux, `open` on macOS, `cmd /c start` on Windows) with protocol validation (`http`, `https`, `mailto`); added `openExternalUrl` frontend helper with URL normalization; replaced default `<a target="_blank">` navigation with native desktop browser launch handlers. | Clicking any external link inside notes, update alerts, or modal dialogues opens directly in the user's active/default desktop browser without opening blank WebView windows or doing nothing. | 1. Click a markdown web link (e.g. `https://google.com`) in note -> verify opens default browser tab.<br>2. Click GitHub repo link in About modal -> verify opens in default desktop browser.<br>3. Run `npm test` and `cargo test` -> verify 100% pass. |
| **Multi-Provider AI Vault, Dynamic Models Catalog & Monotonic UI Refactor** | `src/components/Modals/AISettingsModal.tsx`, `src/services/ai/aiModelsCatalogService.ts`, `src/constants/aiModelsCatalog.ts`, `config/ai-models.json`, `src/lib/storage.ts`, `src/types/index.ts`, `src/components/Modals/AppModals.tsx`, `src-tauri/src/infrastructure/network/ai_client.rs` | Multi-provider credential vault, dynamic GitHub models catalog, 24h local cache, borderless monotonic model chips without scaling, native Rust connection pooling | Added independent AES-256-GCM encrypted credential vault for Gemini, OpenAI, OpenRouter, and Custom/Ollama; implemented 24h-cached GitHub raw JSON models catalog with bundled offline fallback; redesigned modal to use monotonic slate/black/white palette without rainbow borders, removed font scaling on selection, made suggested chips borderless, and styled provider tabs consistent with canvas settings. | Clean, non-cluttered AI settings modal with monotonic design language matching the rest of the application; borderless suggested model chips and history tags; seamless tab navigation. | 1. Open AI Settings -> verify borderless model chips and monotonic design.<br>2. Select a suggested model chip -> verify chip does not scale or jump layout.<br>3. Switch tabs -> verify clean tab design matching canvas preferences.<br>4. Run `npm test` and `cargo test` -> verify 100% pass. |
| **Note Cover & Artistic Vector Seal System** | `src/components/NoteCard/NoteCover.tsx`, `src/components/NoteCard/index.tsx`, `src/components/NoteCard/NoteHeader.tsx`, `src/components/NoteCard/NoteStylePicker.tsx`, `src/components/Modals/CanvasSettingsModal.tsx`, `src/constants/noteCovers.tsx`, `src/types/index.ts`, `src-tauri/src/models/note.rs`, `src-tauri/src/infrastructure/sqlite/`, `src/hooks/useNoteSelection.ts`, `src/App.tsx`, `src/constants/shortcuts.ts` | Full-bleed note cover overlay, 8 distinct cover styles, 10 vector SVG seal emblems, note style picker customization without redundant toggle switch (keybind-driven), dedicated Covers & Seals canvas preferences tab with interactive live preview card, 100% vector SVG icon enforcement (zero emojis), Alt+Click multi-select guard, drag-reveal suppression, Alt+C / Shift+C cover toggle keybind, covered note context menu suppression, selection & edit-mode isolation guards, double-click reveal debounce | Implemented `NoteCover` full-card coverage concealing header, markdown body, and toolbar behind note title, SVG seal emblem, and "Click to open" prompt; streamlined `NoteStylePicker` to customize Cover Style & Seal SVG Emblem directly with keybind indicator (`Alt+C`) while removing the redundant on/off switch; integrated "Close cover" action in `NoteHeader`; created dedicated `Covers & Seals` gallery tab in `CanvasSettingsModal` with real-time interactive preview card; persisted `is_covered`, `cover_style`, `seal_style`, and `cover_prompt` in SQLite; purged all unicode emojis in favor of crisp SVG icons; added pointer coordinate tracking and `isDragging` checks so dragging notes never accidentally reveals covers; added `Alt+C` / `Shift+C` global keybind to toggle cover on/off for selected notes; suppressed all context menus when a note is covered; added `onDoubleClick` capture on `NoteCover` and `lastRevealedTimeRef` 500ms debounce in `NoteCard` to prevent trailing double clicks during reveal from triggering edit mode; enforced strict `canEdit` and `isCoverActive` rendering gates in `NoteCard` and `useNoteSelection` so marquee drag-select, multi-selection, and Enter key shortcuts never accidentally trigger edit mode or reveal textareas on covered notes. | Notes with cover enabled conceal content under an aesthetic cover card; clicking/tapping or double-clicking seamlessly reveals the note without modifying note content, triggering edit mode, or opening textareas; Alt+clicking or dragging a covered note never reveals it; right-clicking a covered note suppresses all context menus; drag-selecting or multi-selecting covered notes selects card bounding boxes without triggering edit mode; pressing Alt+C toggles cover on/off; clicking "Close cover" re-seals the note. | 1. Double click covered note -> verify note reveals without entering edit mode.<br>2. Select note and press Alt+C or Shift+C -> verify cover toggles on/off.<br>3. Drag-select multiple notes including covered notes -> verify notes select cleanly without entering edit mode.<br>4. Drag covered note across canvas -> verify note moves smoothly and does NOT reveal upon release.<br>5. Alt+Click cover -> verify note is NOT revealed.<br>6. Right-click covered note -> verify NO context menu appears.<br>7. Run `npm test`, `npm run lint`, `npm run build`, and `cargo check` -> verify 100% pass. |
| **Vintage Airmail Envelope Note Cover & Postage Stamp Seals** | `src/components/NoteCard/NoteCover.tsx`, `src/components/NoteCard/NoteCoverDecorations.tsx`, `src/components/Modals/CanvasSettingsModal.tsx`, `src/constants/noteCovers.tsx`, `src/assets/note-covers/`, `src/types/index.ts`, `src/components/__tests__/NoteCover.test.tsx` | Vintage Airmail Envelope cover style (`vintage-airmail`), repeating barber-pole diagonal red/blue striped perimeter border, envelope fold crease geometry, top-left "PAR AVION · AIR MAIL" pill badge, top-right perforated miniature postage stamp with wavy postal cancellation marks, 6 new authentic vector postage stamp & cancellation seals (`air-mail-postmark`, `eiffel-postage-stamp`, `pisa-postage-stamp`, `pyramid-postage-stamp`, `vintage-clock-postmark`, `mail-crest-postmark`), dedicated `NoteCoverDecorations` single source of truth component, modular `src/assets/note-covers/` SVG assets mirroring washi tapes architecture | Added `vintage-airmail` cover style with warm ivory aerogramme tone and red/blue barber-pole border; modularized all cover decorative overlays into `NoteCoverDecorations` (used identically in both `NoteCover` and `CanvasSettingsModal`); created standalone SVG vector assets in `src/assets/note-covers/`; added 6 pure vector postage stamp and postmark seal emblems across "Airmail", "Postage", and "Vintage" categories in `noteCovers.tsx`. | Notes with `vintage-airmail` cover style render an authentic vintage airmail envelope with crisp barber-pole striped border, "PAR AVION" tag, and postage stamp; selecting any of the new vector seals renders crisp vector stamp art on the cover; reveals smoothly on click or keypress; single source of truth guarantees 100% visual parity across canvas and preferences preview without duplicated code. | 1. Select note -> choose "Vintage Airmail" in style picker -> verify red/blue barber-pole border and PAR AVION badge.<br>2. Switch seal emblem to "Air Mail Postmark" or "Eiffel Stamp" -> verify crisp vector stamp renders.<br>3. Open Preferences -> "Covers & Seals" -> verify live preview card and all 16 vector seals render.<br>4. Run `npm test`, `npm run lint`, `npm run build`, and `cargo check` -> verify 100% pass. |
| **Complete Cover & Seal SVG Asset Suite & Subfolder Organization** | `src/assets/note-covers/covers/`, `src/assets/note-covers/seals/`, `src/constants/noteCovers.tsx`, `src/components/Modals/CanvasSettingsModal.tsx`, `src/components/NoteCard/NoteStylePicker.tsx`, `src/components/__tests__/NoteCover.test.tsx` | Subfolders for note covers (`src/assets/note-covers/covers/`) and seals (`src/assets/note-covers/seals/`), 9 standalone cover SVG assets, 16 seal SVG assets, asset `src` wiring across all styles, Canvas Preferences cover preview vector asset rendering, 8-column responsive seal grid in Preferences and Card Style Picker | Organized all 9 note cover SVGs into `src/assets/note-covers/covers/` and all 16 seal SVGs into `src/assets/note-covers/seals/`; updated imports in `noteCovers.tsx`; upgraded `CanvasSettingsModal` cover swatches to render vector cover artwork from assets; upgraded seal grid to 8-column layout in both preferences and `NoteStylePicker`. | Canvas Preferences and Card Style Picker display all 9 cover styles and 16 seal emblems cleanly with crisp vector preview images loaded directly from SVG assets; swatches and seal buttons are arranged symmetrically without truncation. | 1. Open Canvas Preferences -> "Covers & Seals" -> verify all 9 covers and 16 seals render from assets.<br>2. Open Card Style Picker on a note -> verify Cover Style dropdown has 9 options and seal grid has 16 emblems.<br>3. Run `npm test`, `npm run lint`, `npm run build`, and `cargo check` -> verify 100% pass. |
| **Seal Collection Pruning & Airmail Envelope Badge Removal** | `src/assets/note-covers/seals/`, `src/assets/note-covers/covers/`, `src/components/NoteCard/NoteCover.tsx`, `src/constants/noteCovers.tsx`, `src/types/index.ts`, `src/App.tsx`, `src/components/Modals/CanvasSettingsModal.tsx`, `src/components/__tests__/NoteCover.test.tsx` | Removed 5 SVG seal emblems (`feather-quill`, `mystic-eye`, `vintage-clock-postmark`, `vintage-stamp`, `wax-seal-crest`), updated `DEFAULT_SEAL_STYLE` to `golden-sun`, removed PAR AVION badge and blue background from `NoteCover.tsx` and envelope SVG assets | Deleted 5 unused SVG files from `src/assets/note-covers/seals/`; removed their definitions and imports from `noteCovers.tsx`, `types/index.ts`, `App.tsx`, and `CanvasSettingsModal.tsx`; removed PAR AVION badge, airplane symbol, and blue background from `NoteCover.tsx`, `vintage-airmail-envelope.svg`, and `cover-vintage-airmail.svg`. | Airmail note cover renders cleanly without the blue PAR AVION badge; seal picker and preferences display only the 11 active curated vector seals; default fallback seal is `golden-sun`. | 1. Open Card Style Picker -> verify 11 seal emblems render without deleted ones.<br>2. Set note cover to Vintage Airmail -> verify no PAR AVION blue badge renders.<br>3. Run `npm test`, `npm run lint`, `npm run build`, and `cargo check` -> verify 100% pass. |
| **Dotted Fold Line Removal & Cover Element Upscaling Suite** | `src/components/NoteCard/NoteCover.tsx`, `src/components/NoteCard/NoteCoverDecorations.tsx`, `src/components/NoteCard/NoteStylePicker.tsx`, `src/components/Modals/CanvasSettingsModal.tsx`, `src/assets/note-covers/covers/vintage-airmail-envelope.svg`, `src/assets/note-covers/covers/cover-vintage-airmail.svg` | Removed dotted diagonal crease fold cross lines from all covers; increased seal emblem size (to 68px), date (to `text-sm font-semibold`), title (to `text-2xl font-bold`), action prompt (to `text-sm font-semibold`), and tags (to `text-xs px-2 py-0.5`) across all covers and settings preview cards | Removed the envelope fold crease SVG overlay from `NoteCoverDecorations.tsx`, `vintage-airmail-envelope.svg`, and `cover-vintage-airmail.svg`; upgraded typography and vector icon sizing across `NoteCover.tsx`, `CanvasSettingsModal.tsx`, and `NoteStylePicker.tsx` to provide clear, high-contrast visual hierarchy and readability on canvas. | Note covers render without distracting dotted diagonal cross lines; titles, dates, seal emblems, action prompts, and hashtags are significantly larger, bolder, and more prominent across light and dark themes. | 1. Create/select a covered note -> verify no diagonal dotted cross creases appear.<br>2. Check title, date, seal emblem, and tags -> verify larger, crisp font and icon sizes.<br>3. Open Canvas Preferences & Note Style Picker -> verify live preview card and seal buttons reflect upscaled dimensions.<br>4. Run `npm test`, `npm run lint`, `npm run build`, and `cargo check` -> verify 100% pass. |
| **Airmail Postmark & Cancellation Removal & Cover Tag Omission** | `src/components/NoteCard/NoteCover.tsx`, `src/components/NoteCard/NoteCoverDecorations.tsx`, `src/assets/note-covers/covers/vintage-airmail-envelope.svg`, `src/assets/note-covers/covers/cover-vintage-airmail.svg`, `src/components/__tests__/NoteCover.test.tsx` | Removed top-right miniature postage stamp (`POST / AIR`) and wavy cancellation lines from airmail envelope design; completely removed tag pills from note cover | Cleaned `NoteCoverDecorations.tsx` to only render the barber-pole border; removed stamp and cancellation lines from `vintage-airmail-envelope.svg` and `cover-vintage-airmail.svg`; omitted tag pills from `NoteCover.tsx` keeping the cover face minimal and centered strictly on title, seal, date, and open action prompt. | Airmail note cover renders a clean paper aerogramme with only the crisp diagonal barber-pole border; note covers no longer display hashtag pills, leaving the center clean and uncluttered. | 1. Set note cover to Vintage Airmail -> verify top right has no miniature stamp or wavy lines.<br>2. Add tags to a covered note -> verify tags do NOT appear on the sealed cover.<br>3. Reveal note -> verify tags appear normally inside the opened note.<br>4. Run `npm test`, `npm run lint`, `npm run build`, and `cargo check` -> verify 100% pass. |
| **Universal Note Cover Theme Parity Invariance (All 9 Styles)** | `src/constants/noteCovers.tsx`, `src/components/NoteCard/NoteCoverDecorations.tsx`, `src/components/NoteCard/NoteStylePicker.tsx`, `src/components/Modals/CanvasSettingsModal.tsx` | Comprehensive cover color persistence across light & dark modes for all 9 note cover styles | Purged all dark-mode background, text, and border overrides (`dark:bg`, `dark:text`, `dark:border`) across all 9 note cover style configurations in `NOTE_COVER_STYLES`, guaranteeing that every artistic cover (Vintage Airmail, Classic Kraft, Vintage Leather, Botanical Linen, Sakura Blush, Vintage Parchment, Monochrome Slate, Obsidian Velvet, Ocean Navy) renders its authentic, pristine designer palette consistently across both light and dark themes without being muted or darkened. | Switching between light and dark themes preserves each cover's exact authentic paper background and contrasting typography without color shifting or darkening. | 1. Open Card Style Picker and switch between all 9 cover styles.<br>2. Toggle between Light and Dark mode on canvas.<br>3. Verify all 9 covers maintain their authentic colors and contrast without turning dark or muddy.<br>4. Run `npm test`, `npm run lint`, `npm run build`, and `cargo check` -> verify 100% pass. |
| **Image Note Cover Integration & Re-Sealing Support** | `src/components/NoteCard/ImageNoteCard.tsx`, `src/components/NoteCard/__tests__/ImageNoteCard.test.tsx` | Full note cover overlay, reveal interaction, keyboard/context-menu suppression, and close-cover toolbar button for image/photo notes | Integrated `NoteCover` component into `ImageNoteCard.tsx`; added `isRevealed` state with automatic reset on `isCovered` toggles; guarded double-click lightbox triggers, caption clicks, context menus, and Enter key editing when covered; added "Close cover" quick action button in floating toolbar (`Book01Icon`) when revealed. | Image notes with cover enabled now completely conceal the photo and caption behind the chosen cover style and seal emblem; clicking/tapping seamlessly reveals the photo; clicking "Close cover" re-seals it; right-click and editing gestures are suppressed while covered. | 1. Select an image note -> choose a cover style and press Alt+C.<br>2. Verify the photo is completely concealed by the cover.<br>3. Click the cover -> verify the photo reveals smoothly.<br>4. Click the "Close cover" icon in the floating toolbar -> verify note re-seals.<br>5. Run `npm test`, `npm run lint`, `npm run build`, and `cargo check` -> verify 100% pass. |
| **Note Cover Collection Pruning (3 Curated Styles)** | `src/constants/noteCovers.tsx`, `src/types/index.ts`, `src/App.tsx`, `src/assets/note-covers/covers/`, `src/components/Modals/CanvasSettingsModal.tsx`, `src/components/__tests__/NoteCover.test.tsx` | Pruned note cover styles down to 3 curated styles (`vintage-airmail`, `classic-kraft`, `obsidian-minimal`) and deleted 6 unused SVG cover files | Deleted 6 unused SVG cover assets (`cover-clean-monochrome.svg`, `cover-leather-journal.svg`, `cover-botanical-linen.svg`, `cover-sakura-blush.svg`, `cover-ocean-navy.svg`, `cover-vintage-parchment.svg`); pruned `CoverStyle` type union and `NOTE_COVER_STYLES` array; updated `DEFAULT_COVER_STYLE` to `vintage-airmail`; updated Canvas Preferences grid to 3 responsive columns. | Canvas Preferences and Card Style Picker present only the 3 curated note cover designs (Vintage Airmail, Classic Kraft, Obsidian Velvet); default fallback cover is `vintage-airmail`. | 1. Open Card Style Picker -> verify Cover Style dropdown contains exactly 3 options.<br>2. Open Canvas Preferences -> "Covers & Seals" -> verify 3 cover swatches render cleanly in a 3-column grid.<br>3. Run `npm test`, `npm run lint`, `npm run build`, and `cargo check` -> verify 100% pass. |
| **Monotonic Geometric Seal Scaling & Canvas GPU Pan Optimization** | `src/components/NoteCard/NoteCover.tsx`, `src/components/NoteCard/NoteCoverDecorations.tsx`, `src/components/NoteCard/index.tsx`, `src/components/NoteCard/ImageNoteCard.tsx`, `src-tauri/src/commands/layout.rs`, `src-tauri/src/commands/mod.rs`, `src-tauri/src/lib.rs`, `src/lib/rustLayout.ts`, `src/lib/__tests__/rustLayout.test.ts`, `src/components/__tests__/NoteCover.test.tsx` | Strictly monotonic geometric mean seal scaling ($\sqrt{w \times h}$ from 56px to 220px), title typography scaling (`text-lg`–`text-4xl`), GPU layer promotion (`transform: translateZ(0)`), `contain: strict`, removal of `transition-all`, memoized `handleReveal` callbacks, and conditional omission of hidden note body rendering | Implemented strictly monotonic geometric mean scaling `Math.sqrt(w * h) * 0.24` ensuring larger notes ALWAYS have strictly larger seals and small notes have small compact seals; removed `transition-all` on covers to eliminate frame drops during canvas pan; memoized `handleReveal` with `useCallback` in `NoteCard` and `ImageNoteCard` to guarantee `React.memo` purity; promoted repeating barber-pole border to GPU compositor layer with `translateZ(0)`; added native Rust `cull_notes_in_frustum` and `compute_batch_drag_snapping`. | Note covers scale their seal emblems and title typography strictly and proportionally with card area without tiny seals on wide cards; panning the canvas is buttery smooth (120 FPS) with zero frame drops, zero style recalculation spikes, and zero redundant re-renders. | 1. Resize covered note from small (280x300) to wide (800x500) -> verify seal emblem grows proportionally without ever shrinking.<br>2. Pan canvas at high speed -> verify buttery smooth 120 FPS motion with zero lag or frame jitter.<br>3. Run `npm test`, `npm run lint`, `npm run build`, and `cargo test` -> verify 100% pass. |
| **Note Cover Canvas Performance & Hardware Layer Flattening** | `src/components/NoteCard/NoteCover.tsx`, `src/components/NoteCard/NoteCoverDecorations.tsx`, `src/components/NoteCard/index.tsx`, `src/components/NoteCard/ImageNoteCard.tsx` | NoteCover root style optimization, SVG pattern fill for vintage airmail border, conditional DOM subtree omission, NoteCard memo comparator cover tracking | Removed nested `transform: translateZ(0)` and redundant `contain: strict` to eliminate excessive GPU compositor layers; replaced CPU-heavy CSS `repeating-linear-gradient` in `NoteCoverDecorations` with a hardware-accelerated SVG `<pattern>` border fill; conditionally unmounted occluded header, markdown AST parser, toolbar, and decorations when `isCoverActive` is true; added `isCovered`, `coverStyle`, `sealStyle`, and `coverPrompt` to `NoteCard`'s `React.memo` comparator. | Canvas pan, zoom, and note dragging with covers enabled operates at 120 FPS with minimal CPU utilization; no software gradient rasterization loops; occluded note bodies do not execute Remark AST parsing or inflate DOM node counts. | 1. Enable cover on multiple notes -> pan and zoom canvas rapidly -> verify 120 FPS buttery smooth motion without lag or CPU spikes.<br>2. Click covered note -> verify reveals instantly.<br>3. Toggle cover on/off -> verify React.memo updates state accurately.<br>4. Run `npm test`, `npm run lint`, and `npm run build` -> verify 100% pass. |
| **Direct Hardware Pan Decoupling & Pointer-Events Suppression** | `src/components/InfiniteCanvas.tsx`, `src/components/NoteCard/NoteCover.tsx`, `src/components/NoteCard/ImageNoteCard.tsx` | Direct DOM transform in requestAnimationFrame during continuous panning, immediate wheel DOM transform preview, pointer-events suppression during pan, seal hover animation purge | Decoupled continuous canvas panning gestures from React virtual DOM re-renders by mutating `worldLayerRef.current.style.transform` directly inside `requestAnimationFrame`; added throttled state sync (120ms) for off-screen culling updates; added instant direct DOM transform preview in `scheduleWheelTransform`; suppressed all pointer events on `worldLayerRef` during active panning to prevent cards gliding under cursor from triggering hover recalculations; purged `hover:scale-105` and transition transforms from note cover seal containers. | Canvas panning and zooming glide at native monitor refresh rates (120/144 FPS) with 0ms input lag; zero React re-render blocking during continuous pan movements; cards gliding under the cursor never trigger hover transitions or style recalculation hiccups. | 1. Middle-click or Space-drag across canvas at high speed -> verify 120 FPS buttery smooth motion with 0 input lag.<br>2. Zoom in/out via wheel -> verify instantaneous responsive scaling.<br>3. Run `npm test`, `npm run lint`, and `npm run build` -> verify 100% pass. |
| **Destructured Tabbed Note Style Picker & Screen Collision Clamping** | `src/components/NoteCard/NoteStylePicker.tsx`, `src/components/NoteCard/__tests__/NoteStylePicker.test.tsx` | Note style popover segmented tab navigation (`Paper & Style`, `Typography`, `Cover & Seal`), modal height reduction (<300px), focused category controls, `shadow-sm` elevation, and dynamic viewport edge collision prevention | Replaced the 650px+ dense vertical scrolling layout with a lightweight 3-tab segmented control architecture; Tab 1 (`Paper & Style` / `Frame & Pins`) groups washi tapes, paper themes, photo framing, and bulletin tilts; Tab 2 (`Typography`) isolates font selection and text sizing; Tab 3 (`Cover & Seal`) isolates cover styles and vector seal emblems with `Alt+C` shortcut indicator; enforced subtle `shadow-sm` elevation; added `useLayoutEffect` viewport bounding-box collision detection to auto-clamp and shift the popover whenever notes approach top (clearing titlebar), bottom, or side window edges. | Note style picker is compact, elegant, non-intrusive (<300px high), and uses subtle `shadow-sm`; never overflows or clips behind the titlebar or screen edges; switching between tabs is instantaneous. | 1. Open Card Style Picker on a note near the top titlebar -> verify popover auto-shifts downwards without colliding with the titlebar.<br>2. Open on a note near canvas edges -> verify popover stays within viewport boundaries.<br>3. Verify subtle shadow-sm is applied.<br>4. Run `npm test`, `npm run lint`, and `npm run build` -> verify 100% pass. |

---

## 2. Mandatory Operational Commands & Tool Rules

> [!CAUTION]
> **RULE #2 (NO DEV SERVER EXECUTION):**
> **DO NOT EVER run `npm run dev`, `bun dev`, `vite`, or launch background development servers.**
> DiaryNote is tested and validated via static checks, unit tests, and production build verification (`npm run lint`, `npm test`, `npm run build`, and `cargo check`). Do not spawn long-running server processes.

> [!IMPORTANT]
> **RULE #3 (ALWAYS CHECK LINT WITH OXLINT):**
> **Always execute `npm run lint` before concluding any task.**
> `npm run lint` is configured with `oxlint` as the default ultra-fast linter combined with TypeScript strict typechecking (`oxlint && tsc --noEmit`).
> Tasks must never be marked complete if `npm run lint` reports any errors.

---

## 3. Core Architectural Invariants for Agents

All agents working on DiaryNote must adhere to these standing principles:

1. **Desktop Native & Offline First:**
   - DiaryNote is a desktop application (Tauri + Rust + React). Do not introduce assumptions of remote web servers, cloud sync, or hosted SaaS infrastructure.
   - All network interactions (updates, AI endpoints) must be strictly user-configurable and default to privacy-preserving boundaries.

2. **Zero-Loss Persistence Protocol:**
   - Never bypass the note repository layer with direct React state overrides.
   - A note is only considered saved after IndexedDB storage confirmation resolves.

3. **Zero-Knowledge Security & Privacy:**
   - Locked notes must be encrypted at rest using Argon2id + AES-256-GCM. Plaintext locked content must never touch persistent storage or unauthenticated memory caches.
   - Exclude locked notes from exports, clipboard copies, AI prompts, and graph indexes unless explicitly authenticated.

4. **CPU & Rendering Performance:**
   - Never call `getBoundingClientRect()`, `offsetWidth`, or `offsetHeight` inside mousemove or touchmove loops.
   - Decouple note metadata from markdown bodies. Heavy search and cryptography operations must run in Web Workers or native Rust commands.

5. **Quality Verification Before Completion:**
   - Every task must pass:
     ```bash
     npm run lint
     npm run build
     cargo check --manifest-path src-tauri/Cargo.toml
     ```
   - Automated tests (`npm test`) must pass with zero errors before any task is marked done.

---

## 4. UI Regression Troubleshooting Guide

If a UI component stops functioning, misaligns, or clips after a task:
1. **Check the Registry Table Above:** Find the file in the registry and review what props, state, or DOM structure were altered in that phase.
2. **Inspect Event Delegation:** Ensure Pointer Events (`onPointerDown`, `setPointerCapture`) are not being blocked by overlapping containers or missing `touch-action: none`.
3. **Check Portal Mounting:** Ensure popovers and floating menus (e.g., Theme/Align menus) use `createPortal(..., document.body)` to prevent container overflow clipping.
4. **Verify CSS Classes:** Verify Tailwind CSS utility classes conform to Tailwind v4 syntax without deprecated arbitrary values.
